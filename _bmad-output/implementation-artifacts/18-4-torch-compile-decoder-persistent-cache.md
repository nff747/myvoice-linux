# Story 18.4: torch.compile Decoder + Persistent Compile Cache (via upstream `enable_streaming_optimizations()` + qwen-tts pin-bump)

Status: review

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->
<!-- Phase tag: Phase ⊥-Polish-2 (D-20). Fourth + final story of Epic 18 (Generation-Speed Optimizations). Successor to Story 18.3 (bf16 precision — closed empirically null on RTX 5090; deferred its NFR3 audition pending this story per OQ #3 option (b)). -->
<!-- Architecture: refines existing Epic 18 stub at `epics-optimization-pass.md:1390-1411` per the new `architecture-streaming-acceleration-and-lightning-tier.md` (sealed 2026-05-10, COMPLETE). Implements D-21 (decode-window=30 fixed), D-22 (qwen-tts pin discipline), D-23 (background warmup + persistent cache), D-24 (7-dim cache key), D-25 (decode-window invariant assertion) + P-10 (single-helper cache key) + P-11 (invariant assertions at startup) + P-12 (capability verification probes). -->
<!-- D-22 EMPIRICAL VERIFICATION ALREADY RESOLVED: 2026-05-10 research subagent confirmed `enable_streaming_optimizations` does NOT exist at the current pin `1ab0dd75` (= QwenLM/Qwen3-TTS upstream HEAD lineage; upstream has zero release tags + 13 commits + zero matches for the symbol). The API was introduced in the community fork `dffdeeq/Qwen3-TTS-streaming` at commit `3fdb4682` ("compile and fast codebook", 2026-02-03). Drop-in replacement: same package name `qwen-tts 0.0.4`, same `qwen_tts.inference.qwen3_tts_model.Qwen3TTSModel` import path. **Branch B fires.** -->
<!-- Audition: per architecture D-22 + parent NFR3 row policy (`architecture-optimization-pass.md:803`) the pin-bump triggers a full Story-17.1-grade ≥3-listener perceptual A/B re-audition before the bump merges. Per architecture D-27.3 + the user's `/bmad-bmm-create-story` direction 2026-05-10, the audition is **JOINT** — single audition certifies the bf16 dimension (deferred from Story 18.3 OQ #3 option (b)) AND the compile-engaged dimension AND the pin-bump dimension. Single fixture regen against bf16+compile+pin-bumped output; ≥3 listeners; same `audible_seam` zero-flag gate as Story 17.1. Audition deliverable at `_bmad-output/implementation-artifacts/18-4-bf16-compile-pinbump-audition.csv`. -->
<!-- Reality check: this story is the load-bearing producer-side fix per `memory/epic18_producer_bottleneck_finding.md` (Story 18.1 ratio 3.23×; Story 18.2 closed it to 1.40 on fp32+TF32; Story 18.3 closed nothing on bf16; Story 18.4 is the last shot before the epic retros with "compile didn't pay either"). Research-quoted upstream gain: 2.15× per-frame on the codebook predictor (12.94 ms → ~6 ms compiled); community-fork TTFA improvements 5–10× when paired with the existing sliding-window streamer. `dynamic=True` (the original epic-stub design) is mutually exclusive with CUDA Graph capture; the architecture binds D-21 to `decode_window_frames=30` fixed. -->

## Story

As a **MyVoice user generating TRUE_STREAM utterances on an Ampere-or-newer CUDA host**,
I want **the qwen-tts decoder + codebook predictor to run under `torch.compile` with `compile_mode="reduce-overhead"` (CUDA Graph replay) using the official upstream `Qwen3TTSModel.enable_streaming_optimizations(decode_window_frames=30, use_compile=True, compile_mode="reduce-overhead")` API, with a persistent compile cache at `%LOCALAPPDATA%/MyVoice/torch_compile_cache/` keyed on the seven dimensions that affect compiled-graph correctness, an "Preparing TTS engine…" indicator on cold compile that mirrors Story 17.2's "Preparing voice…" pattern, and a clean fallback chain to eager-mode generation if any compile / cache / capability stage fails**,
so that **first-chunk latency drops 1.5–3× on warm compile cache (per the upstream-blessed 2.15×-per-frame benchmark + the community-fork 5–10× TTFA evidence in `_bmad-output/planning-artifacts/research/technical-tts-streaming-fast-clone-research-2026-05-10.md`), the producer-bottleneck ratio Story 18.1 measured at 3.23× (reduced to 1.40 by Story 18.2's TF32+cuDNN engagement) closes to <1.0× sustained on RTX 5090, the bf16 path Story 18.3 shipped engaged-but-empirically-null finally pays for itself when composed with kernel-fusion and CUDA Graph replay (re-validating D-27's "bf16's tensor-core advantage may finally materialize once kernel-launch overhead collapses" hypothesis), the cold-compile UX is non-blocking (UI stays responsive; "Preparing TTS engine…" indicator visible per Story 17.2 inline-label precedent), and the joint NFR3 audition (≥3-listener blind A/B against the bf16+compile+pin-bumped output) certifies bf16-as-default + compile-as-default + pin-bump-to-`dffdeeq/Qwen3-TTS-streaming@3fdb4682` simultaneously, closing both Story 18.3's deferred audition and the D-22 Branch-B re-audition obligation in a single listener cycle**.

## Acceptance Criteria

**Given** the architecture document `_bmad-output/planning-artifacts/architecture-streaming-acceleration-and-lightning-tier.md` (sealed 2026-05-10) binds D-22 to a verify-or-bump branch (Branch A: pin `1ab0dd75` ships the API → keep pin + add internals test; Branch B: pin doesn't ship the API → bump pin + trigger NFR3 re-audition)
**When** the dev agent re-verifies the empirical question at the current pin
**Then** the verification confirms the 2026-05-10 research subagent's finding that `Qwen3TTSModel.enable_streaming_optimizations` **does NOT exist** at `qwen-tts @ git+https://github.com/QwenLM/Qwen3-TTS.git@1ab0dd75353392f28a0d05d9ca960c9954b13c83` (verified by `grep -rn "enable_streaming_optimizations\|use_compile\|compile_mode" I:/MyVoiceV2/python310/Lib/site-packages/qwen_tts` returning zero matches); **D-22 Branch B fires**
**And** the verification is captured at `_bmad-output/implementation-artifacts/18-4-torch-compile-decoder-persistent-cache-evidence.md §"D-22 verification (Branch A vs Branch B)"` with the verbatim grep transcript + the upstream HEAD commit `022e286b` (2026-03-17) + the chosen Branch B target pin `dffdeeq/Qwen3-TTS-streaming@3fdb4682` (introducing commit "compile and fast codebook", 2026-02-03)

**Given** D-22 Branch B fires
**When** the pin-bump lands
**Then** `requirements.txt:23` is updated from `qwen-tts @ git+https://github.com/QwenLM/Qwen3-TTS.git@1ab0dd75353392f28a0d05d9ca960c9954b13c83` to `qwen-tts @ git+https://github.com/dffdeeq/Qwen3-TTS-streaming.git@3fdb4682<full-hash>` (full-hash captured at pin time)
**And** `build_tools/requirements-production.txt:61` is updated identically (production-bundle pin discipline per `memory/build_tools_phase_perp_state.md`)
**And** the package name remains `qwen-tts` (the fork's `pyproject.toml` declares `name = "qwen-tts", version = "0.0.4"` — drop-in replacement; no other edits to `requirements.txt` needed)
**And** `tests/test_qwen_tts_internals.py:48-:60` (the `expected_methods` list) is **extended** with `enable_streaming_optimizations` — `assert callable(getattr(Qwen3TTSModel, "enable_streaming_optimizations", None))` succeeds (per architecture P-11: invariant assertion at startup; Branch A's import-attribute test pattern adapted for the bumped pin)
**And** the existing 5 internals tests (`test_package_imports`, `test_module_top_level_attrs`, `test_qwen3_tts_model_methods`, `test_decoder_inner_path`, plus any others in the file) continue to pass against the new pin (regression coverage — the bumped fork does not remove symbols MyVoice depends on)
**And** the `_QWEN_TTS_PIN_HASH = "1ab0dd75"` constant at `qwen_tts_service.py:1150` is **bumped** to the first 8 hex chars of the new fork commit (per Story 17.2's H1+H2 cache-invalidation discipline at `memory/code_review_regression_test_exact_class.md` — bumping the pin without bumping the constant lets stale `voice_clone_prompt` `.pt` files load with a wrong-pin meta sidecar). The bump invalidates Story 17.2's cached `.pt` files cleanly on first run after the pin-bump (cold-recompute path; per Story 17.2 §AC #3 and `qwen_tts_service.py:1505`)

**Given** the pin-bump lands
**When** the broader regression-test sweep runs against the new fork
**Then** the existing Story 16.7 / 16.8 / 17.1 / 17.2 / 17.3 / 18.1 / 18.2 / 18.3 test surface (TRUE_STREAM dispatch + sliding-window forward-hook + voice_clone_prompt cache + progressive playback + dtype audit + tts_precision resolver + 803/803 broader sweep from Story 18.3) passes with **zero regressions** — the fork's added `stream_generate_pcm()` / `stream_generate_voice_clone()` / `enable_streaming_optimizations()` methods are **additive** (research subagent confirmed: same import paths; same `Qwen3TTSModel` class; new methods don't displace existing ones)
**And** the dispatch chain `_dispatch_by_streaming_mode` at `qwen_tts_service.py:3320-3399` continues to route TRUE_STREAM through the existing Story 16.8 talker-patch path (the new `stream_generate_pcm()` API is **NOT** adopted by Story 18.4 — Story 18.4 stays on the existing forward-hook + CodecTokenStreamer + StreamingDecoderWorker pipeline; only `enable_streaming_optimizations()` is wired in. This preserves Story 16.8's audited dispatch chain and lets Story 18.4 ship without re-engineering the streaming pipeline)
**And** the bundled-environment smoke (Story 18.3 §AC #13 / Story 17.2 §AC #6 pattern) runs against the new pin in a fresh `build_release.bat` build; Commander confirms zero new error / warning lines in `myvoice.log` and zero perceptual defects on the canonical Sarira-F long-form utterance compared to the pre-pin-bump baseline (eager-mode generation; compile not yet engaged)

**Given** the pin-bump lands and the API is now callable
**When** the new compile-cache module is created
**Then** `src/myvoice/services/tts_streaming/compile_cache.py` is created with the public surface architecture P-10 mandates:
  - `compute_key(*, qwen_tts_pin_hash: str, model_id: str, precision_str: str, torch_version: str, decode_window_frames: int, cuda_capability: Tuple[int, int], compile_mode: str) -> str` — pure-decision; returns SHA-256 hex digest of the seven dimensions joined by `"|"` (architecture D-24 verbatim). **Side-effect-free** (no logging, no metric emission, no filesystem touch). Mirrors Story 18.2's `is_ampere_or_newer` / Story 18.3's `resolve_tts_precision` pure-decision discipline
  - `cache_root() -> Path` — returns `%LOCALAPPDATA%/MyVoice/torch_compile_cache/` on Windows; `~/.cache/MyVoice/torch_compile_cache/` on Linux/macOS (same `os.environ.get("LOCALAPPDATA")` precedent Story 17.2 uses for voice profiles, with the fallback `Path.home() / ".cache" / "MyVoice"` per parent V2 `architecture.md`'s storage-path discipline). Creates the directory if absent. **Idempotent**
  - `cache_dir_for_key(key: str) -> Path` — returns `cache_root() / key[:16]` (16-char prefix as a directory; collision-resistant within a single user's cache; full key in a `meta.json` sidecar inside the directory). Mirrors Story 17.2's `_voice_clone_prompt_persist_paths` per-key directory pattern
  - `is_warm(key: str) -> bool` — returns True iff `cache_dir_for_key(key) / meta.json` exists AND its `key` field matches the input (defensive against directory-collision; full-key match in meta is the canonical signal). Mirrors Story 17.2's `_voice_clone_prompt_meta_is_valid` discipline
  - `mark_warm(key: str) -> None` — writes `cache_dir_for_key(key) / meta.json` with `{"key": key, "warmed_at": <ISO8601>, "torch_compile_cache_dir": <str>}` and creates the directory. Caller (the warmup worker) writes this AFTER successfully running a compile-priming generation (architecture D-23: warm = "compile-priming generation completed without error")
  - `set_torchinductor_cache_dir(key: str) -> None` — sets `os.environ["TORCHINDUCTOR_CACHE_DIR"] = str(cache_dir_for_key(key))` BEFORE the `model.enable_streaming_optimizations(...)` call so PyTorch's inductor cache writes/reads from the per-key directory. Idempotent (overwrites on re-call). Logs at INFO once per process
**And** the module is exported via `src/myvoice/services/tts_streaming/__init__.py` alongside Story 18.2's `enable_tf32_and_cudnn_benchmark` / `is_ampere_or_newer` and Story 18.3's `resolve_tts_precision` exports
**And** unit tests at `tests/unit/services/tts_streaming/test_compile_cache.py` exercise:
  - **P-10 key stability:** `compute_key(...)` with fixed inputs returns the same SHA-256 digest across two invocations + across two python processes (the architecture's "key stability" obligation at line 626)
  - **P-10 key uniqueness:** varying any single dimension (each of the seven) changes the output digest. Seven parametrized rows, one per dimension. Mirrors Story 18.2's `test_telemetry_tag_schema_*` parametrization style
  - **`cache_root()` Windows path:** monkeypatches `os.environ["LOCALAPPDATA"]` and `sys.platform`; asserts `cache_root()` returns `<LOCALAPPDATA>/MyVoice/torch_compile_cache`
  - **`cache_root()` non-Windows fallback:** monkeypatches `sys.platform = "linux"`; asserts `cache_root()` returns `<home>/.cache/MyVoice/torch_compile_cache`
  - **`is_warm() / mark_warm()` round-trip:** mark_warm + is_warm returns True; cleared directory + is_warm returns False
  - **`is_warm()` defends against directory collision:** `mark_warm(key1)` then `is_warm(key2)` where `key2[:16] == key1[:16]` returns False (prefix collision; the full-key match in meta.json is the gate)
  - **`set_torchinductor_cache_dir()` idempotent:** two calls leave `os.environ["TORCHINDUCTOR_CACHE_DIR"]` equal to the most recent path; logs INFO once

**Given** `compile_cache.py` and the bumped pin land
**When** the dev agent wires `enable_streaming_optimizations` into the model-load path
**Then** `src/myvoice/services/tts_streaming/torch_runtime.py` (the Story 18.2 + 18.3 module) gains a new function `engage_compile_optimizations(model: Any, *, app_settings: Optional[Any] = None, streamer_chunk_size: int = 25, streamer_lookahead: int = 5) -> dict` that:
  1. **Hardware gate (D-9):** returns `{"engaged": False, "reason": "cuda_unavailable"}` early if `is_ampere_or_newer()` is False. CPU and pre-Ampere users skip compile entirely (parent NFR12 inheritance — D-9 hardware-aware default discipline applies to compile dimension just as it does to TF32 / bf16 in Stories 18.2 / 18.3)
  2. **AppSettings gate:** reads `app_settings.tts_compile` (NEW field — see AC below). If `"off"`, returns `{"engaged": False, "reason": "user_disabled"}` early. If `"auto"` or `"on"` or `None`, proceeds. Gate uses `getattr(app_settings, "tts_compile", None)` for defensive access (mirrors Story 18.3's `getattr(app_settings, "tts_precision", None)` precedent at `model_registry.py:137`)
  3. **D-22 Branch A invariant assertion (now a defensive check post-pin-bump):** `assert callable(getattr(model, "enable_streaming_optimizations", None)), "qwen-tts pin lacks enable_streaming_optimizations(); the pin-bump did not propagate."` — mirrors architecture P-11 "loud at startup" pattern; the pin-bump should have closed this branch but the assertion catches a hypothetical regression where the pin reverts silently
  4. **D-25 decode-window invariant:** computes `decode_window_frames = streamer_chunk_size + streamer_lookahead` (= 30 today) and asserts the value matches the streamer's expected window. The streamer constants are read from `codec_token_streamer.DEFAULT_CHUNK_SIZE` (= 25) + `codec_token_streamer.DEFAULT_LOOKAHEAD` (= 5); if the streamer is later re-tuned, the call site provides explicit values. **Hard-fail on mismatch:** `raise AssertionError(f"Decode-window drift: streamer expects {expected}, request asked for {decode_window_frames}. CUDA Graph replay would produce wrong audio.")`
  5. **Cache key + cache dir:** computes `key = compile_cache.compute_key(...)` with all seven dimensions (qwen_tts_pin_hash from `_QWEN_TTS_PIN_HASH` post-bump, model_id from the model attribute, precision_str = "bf16" if `model.dtype == torch.bfloat16` else "fp32", torch_version = `torch.__version__`, decode_window_frames = 30, cuda_capability from `torch.cuda.get_device_capability()`, compile_mode = "reduce-overhead"). Calls `compile_cache.set_torchinductor_cache_dir(key)` BEFORE the API call so the inductor cache reads/writes from the per-key directory
  6. **API call:** invokes `model.enable_streaming_optimizations(decode_window_frames=30, use_compile=True, compile_mode="reduce-overhead")`. Wrapped in `try/except` — on any `TypeError` (signature mismatch from a hypothetical fork update) or `RuntimeError` (compile failure), catches the exception, returns `{"engaged": False, "reason": "compile_failed", "error": str(exc)}`, logs at WARNING, emits the `tts_compile_engaged` telemetry metric with value=0.0 + reason. **NFR7 graceful degradation extension:** the eager-mode generation path stays available; the dispatch chain at `qwen_tts_service.py:3320-3399` is unaffected (compile is a model-state mutation; the dispatch chain reads the same model object regardless of compile state)
  7. **P-12 capability probe:** after the API returns, probes whether compile actually engaged. The exact probe surface depends on what `enable_streaming_optimizations` mutates — verified at impl time by reading the fork's source at `dffdeeq/Qwen3-TTS-streaming@3fdb4682` (the introducing commit's diff: `qwen_tts/inference/qwen3_tts_model.py` + `qwen_tts/core/models/modeling_qwen3_tts.py`). Likely candidates: a `model._streaming_optimizations_engaged` flag (or similar private attribute), an `inspect.unwrap` round-trip showing `torch._dynamo.optimize` wrapping. Probe's failure → returns `{"engaged": False, "reason": "probe_failed"}` (semantically: the API didn't raise but didn't take effect — same NFR7 fallback as compile_failed)
  8. **Telemetry (per architecture P-15 + parent D-19 / P-9):** one `metrics.record("tts_compile_engaged", 1.0 if engaged else 0.0, source="...", reason=..., decode_window_frames=30, cuda_capability="<major>.<minor>", cache_warm=is_warm_at_call)` call (mirrors Story 18.2's `tf32_cudnn_benchmark_enabled` / Story 18.3's `tts_precision_resolved` schema verbatim). The `reason` tag enumerates: `"engaged_warm_cache"`, `"engaged_cold_compile"`, `"cuda_unavailable"`, `"pre_ampere"`, `"user_disabled"`, `"compile_failed"`, `"probe_failed"`
  9. **INFO log line:** `"torch.compile + CUDA Graph engaged (decode_window_frames=30, cuda_capability=<cap>, cache=<warm|cold>)"` on success; `"torch.compile skipped: <reason>"` on each non-engaged branch. Mirrors Story 18.2's `enable_tf32_and_cudnn_benchmark` log discipline

**And** the function is exported via `tts_streaming/__init__.py` alongside the Story 18.2/18.3 exports

**Given** `engage_compile_optimizations` is wired
**When** `ModelRegistry._load_model_sync` (`model_registry.py:438+`) finishes loading the model
**Then** the call site at `model_registry.py:_load_model_sync` (after `from_pretrained` returns + after the existing `torch.set_float32_matmul_precision('high')` / `enable_tf32_and_cudnn_benchmark()` calls) invokes `engage_compile_optimizations(model, app_settings=self._app_settings, streamer_chunk_size=DEFAULT_CHUNK_SIZE, streamer_lookahead=DEFAULT_LOOKAHEAD)` — **wired AFTER all other startup-side `torch.backends.*` engages, BEFORE the model is registered as ready** so the warmup worker (next AC) has the post-compile model available
**And** `ModelRegistry.__init__` accepts a new `app_settings: Optional[Any] = None` parameter that is **propagated into** `_load_model` / `_load_model_sync` (existing methods may need a small signature widen — the parameter already lands on `__init__` per Story 18.3 wire-up at `model_registry.py:100-:155`; Story 18.4 widens the inner methods to read `self._app_settings` — set in `__init__` if not already present in Story 18.3's pass)
**And** the existing INFO log line at `model_registry.py:198-:201` is **extended** with `compile_engaged='{state}'` (one of `True | False | "skipped"`) so Commander can confirm at runtime which path engaged. The extension is append-only (preserves backward-compat for any log-parsing consumer)

**Given** the new `tts_compile` AppSettings field is needed
**When** the field's contract lands at `src/myvoice/models/app_settings.py`
**Then** the field accepts exactly three string values — `"auto"` (default), `"on"`, `"off"` — with semantics:
  - `"auto"` (default) → defers to the hardware probe + compile-failure fallback. Engages on Ampere+ CUDA when `enable_streaming_optimizations` is callable; falls through to eager on any failure
  - `"on"` → user-explicit opt-in; engages on Ampere+ CUDA. Same hardware gate (D-9) as `"auto"` — "on" doesn't override the CPU/pre-Ampere skip; CPU users requesting `"on"` still get eager mode, with a WARNING log + telemetry breadcrumb (`reason="pre_ampere_user_explicit"`)
  - `"off"` → user-explicit opt-out (NFR7 fallback path; engages eager-mode even on Ampere+ when the user has observed a compile-related defect post-release). Mirrors Story 18.3's `tts_precision = "fp32"` NFR7 fallback semantic
**And** the field validation mirrors `streaming_mode_override` / `tts_precision` patterns at `app_settings.py:103-:108` + `:375-:389` exactly: warn-and-fallback to `"auto"` on unknown values; structured `ValidationIssue` with `code="UNKNOWN_TTS_COMPILE"`
**And** the field is persisted in `to_dict` + restored in `from_dict` (mirroring `tts_precision` at `app_settings.py:511 + :527 + :593`)
**And** the field is added to the inline field-name list inside `reset_to_defaults()` (the `for field_name in [...]` loop where `tts_precision` already appears post-Story-18.3)
**And** no UI surface for the setting lands in this story (data-only field; UI exposure is deferred — hand-edit `settings.json` is the user remediation path, mirroring Story 18.3's `tts_precision` precedent)
**And** new unit tests at `tests/unit/models/test_app_settings_tts_compile.py` (extending the Story 18.3 `test_app_settings_tts_precision.py` mirror pattern) cover: (a) default = `"auto"`, (b) explicit `"on"` / `"off"` accepted, (c) round-trip through `to_dict` + `from_dict`, (d) invalid value triggers `UNKNOWN_TTS_COMPILE` ValidationIssue + resets to `"auto"`, (e) `reset_to_defaults` resets `tts_compile` back to `"auto"`. **Mirror Story 18.3's `tts_precision` test row structure exactly** — the bug class is "validation drift between two near-identical fields," which is exactly what `memory/code_review_regression_test_exact_class.md` instructs to mirror

**Given** D-23 mandates background warmup at app start with persistent compile cache
**When** the warmup worker is wired
**Then** a new method `QwenTTSService.warmup_compile_async()` (or analogous; final placement is `qwen_tts_service.py` since that owns the model lifecycle) is added that:
  1. Checks `compile_cache.is_warm(key)` (where key is computed via `compile_cache.compute_key(...)` with current pin/dtype/torch/window/cap/mode). If warm → emits one INFO log line `"Compile cache hit; skipping warmup priming"` + records telemetry `tts_compile_warmup_priming(value=0.0, reason="cache_hit")` + returns. **No priming generation.** This is the steady-state path after the first run
  2. If cold → fires the `_emit_preparing_voice_callback` (or analogous indicator hook) with the message `"Preparing TTS engine…"` (mirrors Story 17.2's pattern at `qwen_tts_service.py:_emit_preparing_voice` which Story 18.3 didn't touch). Indicator clears when the priming generation completes successfully
  3. Runs **one synthetic priming generation** — a short text (≤30 chars) + a non-clone voice (default Sarira-F is acceptable; the priming is about populating the inductor cache, not exercising voice cloning). The generation flows through the same TRUE_STREAM dispatch path the user-facing generation will take; the talker's first forward pass triggers `torch.compile`'s graph capture and the inductor compile that PyTorch's cache directory absorbs
  4. After the priming generation completes (or fails), `compile_cache.mark_warm(key)` is called only on **success**; on failure, the cache stays cold (the next run retries the warmup)
  5. The warmup worker is **fire-and-forget**: dispatched as an asyncio task by `app.py` after the TTS service is constructed (mirrors Story 17.2's `hydrate_voice_clone_prompt_cache` fire-and-forget hook at `app.py:514-:524`). Failure is logged at WARNING but does not block the app
  6. Telemetry at warmup-task level: `metrics.record("tts_compile_warmup_priming", value, reason=..., duration_ms=...)` — `value=1.0` on cache-cold-priming-success, `value=0.0` on cache-hit OR priming-failure. `reason` enumerates: `"cache_hit"`, `"primed_cold"`, `"priming_failed"` (caller distinguishes via the value)
**And** the `app.py` orchestrator's TTS-service-construction site (around `app.py:445-:460` per Story 18.3's H1 fix) wires the warmup callback after `set_preparing_voice_callback` — `self._tts_service.warmup_compile_async()` dispatched to `asyncio.create_task(...)` post-construction
**And** the lazy fallback path (D-23 fallback): if for any reason the background warmup is skipped (env var `MYVOICE_DISABLE_COMPILE_WARMUP=1`, headless test mode, the warmup task fails before reaching the priming generation), the FIRST user-facing generation triggers compile inline. The "Preparing TTS engine…" indicator stays up for the duration; this is acceptable degraded UX (architecture D-23 names it explicitly as the fallback path, not steady-state)

**Given** the warmup worker is wired
**When** unit tests at `tests/unit/services/test_qwen_tts_service_compile_warmup.py` (NEW — per architecture's test-additions table) cover the warmup lifecycle
**Then** the tests exercise:
  - **Cache-hit path:** monkeypatch `compile_cache.is_warm` → True; assert no priming generation runs; assert telemetry records `reason="cache_hit"` with value=0.0
  - **Cache-miss + priming-success path:** monkeypatch `is_warm` → False, mock the model's `generate(...)` (or equivalent priming surface) to succeed; assert the indicator callback fires with `"Preparing TTS engine…"` then clears with None; assert `mark_warm` is called; assert telemetry records `reason="primed_cold"` with value=1.0
  - **Cache-miss + priming-failure path:** monkeypatch `is_warm` → False, mock generate(...) to raise; assert indicator clears; assert `mark_warm` is NOT called; assert telemetry records `reason="priming_failed"` with value=0.0; assert WARNING log emitted
  - **Lazy-fallback gate:** set `MYVOICE_DISABLE_COMPILE_WARMUP=1`; assert `warmup_compile_async` returns early without calling `is_warm` or the priming surface; assert telemetry records the skip
  - **Hardware-gated skip:** monkeypatch `is_ampere_or_newer` → False; assert warmup returns early; assert telemetry records `reason="cuda_unavailable"` or `"pre_ampere"`

**Given** the bundled-environment smoke remains the production-verification gate
**When** the dev agent runs the bundled smoke after the source-tree edits + the pin-bump
**Then** Commander runs `build_release.bat` (per `memory/build_tools_phase_perp_state.md` + `memory/production_release_state.md`); the resulting `build_tools/dist/MyVoice/MyVoice.exe` includes:
  - The bumped `qwen-tts` pin (verified by inspecting `_internal/qwen_tts/inference/qwen3_tts_model.py` for `enable_streaming_optimizations` symbol presence)
  - The new `tts_streaming/compile_cache.py` module
  - The widened `tts_streaming/__init__.py` exports
  - The widened `model_registry.py` (compile wire-up at `_load_model_sync`)
  - The widened `app_settings.py` (`tts_compile` field)
  - The widened `qwen_tts_service.py` (`warmup_compile_async`)
  - The widened `app.py` (warmup callback wire-up)
**And** the bundled exe's `myvoice.log` (in the portable Logs path per `setup_logging()` discipline) contains:
  - The Story 18.2 INFO line: `"TF32 + cuDNN benchmark enabled (device_capability=...)"`
  - The Story 18.3 INFO line: `"ModelRegistry initialized: device=cuda:0, dtype=torch.bfloat16, precision_source='app_settings_auto_ampere', quality_tier=quality, compile_engaged=True"` (extended with the new field per the AC above)
  - The new Story 18.4 INFO line: `"torch.compile + CUDA Graph engaged (decode_window_frames=30, cuda_capability=12.0, cache=cold)"` on first launch; `cache=warm` on subsequent launches
  - The new Story 18.4 INFO line for the warmup priming: `"Compile cache hit; skipping warmup priming"` (subsequent launches) OR `"Compile warmup primed cache successfully (duration=<ms>ms)"` (first launch)
**And** Commander confirms zero new error / warning lines compared to the pre-Story-18.4 baseline (Story 18.3's bundled smoke)
**And** Commander confirms the cold-compile UX: on first launch, the "Preparing TTS engine…" indicator is visible for the warmup duration (target: <30s per architecture's 10–30s budget) and clears cleanly when the priming generation completes; UI stays responsive (NFR4 — UI thread <200ms latency on user input) throughout
**And** Commander confirms perceptual equivalence on the canonical Sarira-F long-form utterance compared to the Story 18.3 baseline — the compile-engaged production-bundled artifact passes the Commander-solo spot-check independent of the multi-listener audition (which gates the architectural amendment, not the bundled smoke)
**And** the bundled-smoke evidence is captured at `_bmad-output/implementation-artifacts/18-4-torch-compile-decoder-persistent-cache-evidence.md §"Bundled smoke"` — the same Story 17.3 / 18.1 / 18.2 / 18.3 evidence-file pattern

**Given** the source-tree edits + pin-bump land
**When** Commander runs the canonical Story 17.3 §4.1 step 3 long-form CLONED utterance (Sarira-F, ≥250 chars / ~22 s of speech) on the RTX 5090 dev host with the same `MYVOICE_PROGRESSIVE_PLAYBACK_CSV` env-var-gated capture infrastructure (`src/myvoice/observability/progressive_playback_csv_capture.py`) in **three configurations**:
  - **A:** `tts_precision="bf16"` + `tts_compile="on"` (the new bf16+compile target)
  - **B:** `tts_precision="bf16"` + `tts_compile="off"` (the bf16 baseline Story 18.3 closed; no compile)
  - **C:** `tts_precision="fp32"` + `tts_compile="off"` (the strict-eager baseline)
**Then** the captured `metrics.first_chunk_latency_ms` value (already-aggregated by `_FirstChunkLatencyAggregator` at `qwen_tts_service.py:362`) is compared head-to-head across all three branches at N=10 generations per branch (matches Story 18.3's discipline; supersedes Story 18.2's deviation single-shot — compile is a larger numerical change than bf16 and the audition gate is load-bearing, so the full N=10 is required)
**And** the measurement is captured at `_bmad-output/implementation-artifacts/18-4-torch-compile-decoder-persistent-cache-evidence.md §"NFR1 first-chunk-latency measurement (3-way A/B/C)"` with three raw consolidated CSVs (`18-4-rtx5090-bf16-compile.csv` + `18-4-rtx5090-bf16-eager.csv` + `18-4-rtx5090-fp32-eager.csv`), median + p90 + p95, and the absolute + percent deltas: A vs B (compile gain), A vs C (compounded gain), B vs C (bf16-only re-validation)
**And** the producer-bottleneck steady-state ratio is computed for all three branches (Story 18.1 §4.4 pattern); pre-Story-18.4 baselines were 1.62× (bf16-eager) and 1.40× (fp32-eager+TF32) per Story 18.3's measurement; Story 18.4's ratio for branch A should drop below 1.0× sustained per the architecture's OFR-E acceptance gate
**And** the captured percent delta is surfaced verbatim to Commander in the closure note. The Epic 18 stub at `:1403` quotes a 1.5–3× warm-cache decode speedup as the *anticipated* gate; if the measured median speedup (A vs B) falls below 20% on first-chunk-latency OR if the producer ratio (branch A) does not drop below 1.0× sustained, route to **Open Question #1** BEFORE running the audition — a sub-20% speedup may indicate the compile didn't apply to the talker's autoregressive loop (where Story 18.1's bottleneck lives) and a different compile target is needed; running an expensive ≥3-listener audition on a half-engaged compile path would be wasted effort. Same OQ-routing precedent Story 18.3 set with its OQ #3
**And** the warm-cache vs cold-cache distinction is captured: branch A's first run is cold-compile (slow startup); subsequent N-1 runs are warm-cache. The aggregator script accounts for the cold-compile outlier (first run discarded from the median calculation; documented separately)

**Given** Story 18.3 deferred its NFR3 audition pending Story 18.4 results (per Story 18.3 OQ #3 option (b), 2026-05-10 closure)
**And** the user's `/bmad-bmm-create-story` direction 2026-05-10 selected option "Joint audition: 18.3 (bf16) + 18.4 (compile) certified together"
**And** D-22 Branch B's pin-bump separately triggers a re-audition obligation per `architecture-optimization-pass.md:803`
**When** the joint audition runs
**Then** the audition is **single-cycle** and certifies all three dimensions simultaneously:
  - bf16 vs fp32 (closes Story 18.3's deferred Task 8)
  - compile-engaged vs eager (closes Story 18.4's primary perceptual gate)
  - pin `dffdeeq/Qwen3-TTS-streaming@3fdb4682` vs the previous pin `QwenLM/Qwen3-TTS@1ab0dd75` (closes D-22 Branch B's re-audition obligation)
**And** the audition fixture is **regenerated** at `_bmad-output/implementation-artifacts/18-4-perceptual-fixtures/` with the 10-pair perceptual-difficult utterance subset (`s-014/15/16/17`, `m-011/12/13/14`, `l-013/14`) — same fixture composition as Story 17.1 / Story 18.3 spec — produced under the new pin + bf16 + compile-engaged. The "A" rendition baseline is regenerated under the new pin + fp32 + eager (the strictest baseline available post-pin-bump); the "B" rendition is the new pin + bf16 + compile (the production target). Fresh `_perlistener_truthtable.json` with L1/L2/L3 randomizations + verbatim copy of `LISTENING-INSTRUCTIONS.md` (same controlled defect vocabulary)
**And** total trials = 30 (10 utterances × 3 listeners × A and B each — labeled blind per Story 17.1's helper-driven protocol, with the same `17-1-l1-audition-helper.py` adapted as `18-4-l1-audition-helper.py` to point at the new fixture directory)
**And** the verdict gate is **verbatim** Story 17.1's: PASS iff zero listeners flag `audible_seam` on any TRUE_STREAM (= bf16 + compile + new pin) pair across all 30 trials
**And** the audition deliverable is the joined CSV at `_bmad-output/implementation-artifacts/18-4-bf16-compile-pinbump-audition.csv` (Story 17.1 / 17.3 / 18.3 evidence-file precedent; force-add per `memory/git_repo_state.md` since `_bmad-output/` is gitignored)
**And** the verdict-computation tables at evidence file §"NFR3 joint audition verdict" mirror Story 17.1's structure verbatim — per-system defect-flag count table; per-listener subtotals; per-utterance subtotals; explicit `audible_seam` zero-flag check; PASS / FAIL outcome
**And** if the audition flags `audible_seam` on any utterance from any listener, the verdict is FAIL → outcome (b) per Story 17.1's framing; the dev agent does NOT unilaterally close the story; route to **Open Question #2** with the failed-utterance class (sibilants vs tonal peaks vs low-amplitude consonants) AND the failed-dimension class (which of bf16 / compile / pin-bump correlates with the defect — this requires a follow-up A/B test that varies one dimension at a time on the failed utterance) annotated. Commander decides whether to (i) defer the bf16-default flip + ship compile-as-default + roll back the pin-bump (only one dimension fails), (ii) defer the bf16-default + compile-default flips and ship `tts_compile = "off"` + `tts_precision = "fp32"` as the new defaults (multiple dimensions fail), (iii) escalate to upstream `dffdeeq` fork investigation, (iv) accept the defect with a session-class fp32-eager fallback for the affected utterance class

**Given** the audition outcome (PASS or FAIL)
**When** the architecture amendment lands
**Then** `_bmad-output/planning-artifacts/architecture-optimization-pass.md` is amended in two places (mirroring the Story 17.1 / Story 18.3 two-place edit pattern):
  - **Place 1:** the NFR3 cell (currently at `:803`: `"...*(Story 18.3 bf16 audition DEFERRED 2026-05-10 pending Story 18.4 producer-bottleneck close — measured no speedup over Story 18.2 fp32+TF32 baseline; revisit post-18.4. See follow-up note below.)*"`) gets the existing parenthetical **superseded** by `*(Story 18.4 joint audition <PASSED|FAILED> {{audition_date}} — certified bf16 + compile + pin-bump from QwenLM/Qwen3-TTS@1ab0dd75 to dffdeeq/Qwen3-TTS-streaming@3fdb4682. See follow-up note below.)*`. The previous Story 18.3 parenthetical is **kept** (as a historical pointer) — the new Story 18.4 parenthetical follows it on the same line, mirroring the cumulative pattern Story 17.1's parenthetical established
  - **Place 2:** a new section titled `#### Story 18.4 Follow-up Note (Joint bf16 + Compile + Pin-Bump Audition, {{audition_date}})` is added immediately after the existing `#### Story 18.3 Follow-up Note` block; the new section captures: (a) the pin-bump rationale + fork commit hash + diff scope + drop-in-replacement evidence (the 2026-05-10 research subagent's verdict), (b) the fixture regeneration methodology, (c) the per-system defect-flag count table verbatim from `18-4-bf16-compile-pinbump-audition.csv`, (d) per-listener + per-utterance subtotals, (e) the verdict-gate computation, (f) the verdict outcome (PASS / FAIL), (g) the architectural decision (bf16+compile-as-default certified post-pin-bump on Ampere+, OR rolled-back-pending-routing), (h) the methodology limitations section verbatim from Story 17.1's pattern (single-room listening environment, single-scribe prompt-framing risk, L1 anonymization not preserved — adapt or reuse Story 17.1's wording as appropriate), (i) source artifacts list (✓ for git-tracked, ○ for gitignored fixture)
**And** `_bmad-output/planning-artifacts/architecture-streaming-acceleration-and-lightning-tier.md` is amended to mark D-22 Branch B as **EXECUTED** (the architecture document said Branch B "would trigger" a re-audition; this story closes the trigger)
**And** the architecture amendment is the **final** artifact in the story closure sequence — i.e., the dev agent does not amend the architecture before the audition lands; the audition outcome is the input to the amendment

**Given** the test suite runs after the source-tree edits
**When** the regression sweep executes
**Then** the existing Story 18.2 + 18.3 `test_torch_runtime` tests (~28 rows post-Story-18.3 — 15 pre-existing + 13 resolver) pass with **zero regressions** (the new `engage_compile_optimizations` function lives in the same module — must not regress the existing surface)
**And** new unit tests at `tests/unit/services/tts_streaming/test_torch_runtime.py` (extending the existing file, NOT a parallel file) cover all eight `engage_compile_optimizations` branches enumerated in AC #4 (the `reason` enum):
  - `cuda_unavailable` (monkeypatched `torch.cuda.is_available` → False)
  - `pre_ampere` (monkeypatched device capability < 8.0)
  - `user_disabled` (`app_settings.tts_compile = "off"`)
  - `engaged_warm_cache` (monkeypatched `compile_cache.is_warm` → True; mock `model.enable_streaming_optimizations` succeeds; probe succeeds)
  - `engaged_cold_compile` (monkeypatched `is_warm` → False; mock API succeeds; probe succeeds)
  - `compile_failed` (mock API raises `RuntimeError`; assert eager fallback returned)
  - `probe_failed` (mock API succeeds but probe attribute missing; assert eager fallback returned)
  - `pre_ampere_user_explicit` (`tts_compile = "on"` + monkeypatched cap < 8.0; assert WARNING log)
**And** the new `test_compile_cache.py` covers the seven test rows enumerated in AC #3
**And** the new `test_app_settings_tts_compile.py` covers the five test rows enumerated in the AppSettings AC
**And** the new `test_qwen_tts_service_compile_warmup.py` covers the five test rows enumerated in the warmup-worker AC
**And** the existing Story 18.1 instrumentation tests (23 tests; Story 18.3 closed at 23/23) pass with zero regressions
**And** the existing Story 17.3 progressive-playback tests (35 tests post-Story-18.3) pass with zero regressions
**And** the existing Story 16.2 streaming-mode tests (16 tests post-Story-18.3) pass with zero regressions
**And** the existing Story 17.2 voice_clone_prompt cache tests pass with zero regressions, **AND** the `_QWEN_TTS_PIN_HASH` constant bump invalidates Story 17.2's cached `.pt` files cleanly on first run after the pin-bump (the existing meta-validation test at `test_voice_clone_prompt_cache.py` (or equivalent) covers the pin-mismatch invalidation path; if not, add ONE regression test that constructs a `.pt.meta.json` with the OLD pin hash and asserts `is_valid` returns False)
**And** the broader Epic 16 / 17 / 18 regression sweep at the Story 18.3 broader-sweep target of **803/803 PASS** lifts to ~825+ (the new tests from Story 18.4 add ~20–25 rows). Exact count is informational; the contract is "all AC sub-bullets have at least one passing test that exercises them"
**And** the two pre-existing tests pinning the exact `tts_streaming/__init__.py` `__all__` ordering (declaration-order discipline at `test_codec_token_streamer.py::test_package_all_lists_expected_symbols_in_order` + `test_streaming_decoder.py::test_package_all_lists_expected_symbols_in_declaration_order`) are updated to append the new symbols (`engage_compile_optimizations` + the `compile_cache` module re-exports if any) per Story 18.2 / 18.3's append-only widening precedent

**Given** the story is closed
**When** the post-implementation accounting runs
**Then** the change log records the absolute + percent first-chunk-latency delta (branches A, B, C above) so future stories can baseline against the post-Story-18.4 production state
**And** Commander handles the build-counter increments at `build_tools/installer.iss` + `build_tools/version.py` in a separate build-state commit per the Story 18.2 OQ #4 / Story 18.3 precedent (from the `memory/build_tools_phase_perp_state.md` discipline). `build_tools/installer.iss:MyAppBuild` and `build_tools/version.py:VERSION_BUILD` are bumped from `12` (Story 18.3's bundled-smoke build) to `13` (Story 18.4's bundled-smoke build); these edits are **NOT** part of Story 18.4's source-tree commit
**And** `requirements.txt:23` and `build_tools/requirements-production.txt:61` are bumped to the new fork pin **as part of Story 18.4's source-tree commit** (the pin is the architecturally-load-bearing change, not an installer-spec edit). Per Epic 18 framing at `:248` ("18.1–18.3 are pure source-tree edits") — Story 18.4 IS NOT subject to that constraint because it implements D-22 Branch B which is explicitly the pin-bump branch. The requirements files ARE source-tree-equivalent for pin purposes; the installer-spec edit (the `MyAppBuild` increment) stays Commander-routed
**And** the architecture document at `_bmad-output/planning-artifacts/architecture-optimization-pass.md` IS amended (per the AC above — the two-place edit), AND `architecture-streaming-acceleration-and-lightning-tier.md` is amended at D-22 to mark Branch B as **EXECUTED**. Per Epic 18 framing at `:234` ("No new D-decisions") + the new architecture's framing: D-22 (qwen-tts API pin discipline) is the architecture-defined branch this story executes, NOT a new D-decision; D-9 / NFR3 / NFR7 / NFR12 are preserved (NFR7 extended by `tts_compile` setting; NFR3 re-cleared by joint audition)
**And** the `memory/build_tools_phase_perp_state.md` HIGH follow-up tracker is updated: the TRUE_STREAM voice_clone_prompt regression class (mentioned in `tooling-2-build-tools-audit-evidence.md §7.2`, closed by Story 17.2 / 17.3 with one residual ambiguity) is re-validated against the bumped pin; if the bumped pin's `stream_generate_voice_clone()` API is structurally equivalent to the pre-bump path, no further work; if it diverges, a follow-up Story 18.5 or a memory-entry note (whichever scope-fits) is added
**And** the `memory/epic18_producer_bottleneck_finding.md` memory entry is **updated** with Story 18.4's measurement outcome — the memory currently names the producer ratio at 3.23× and identifies 18.3 + 18.4 as the fix class; Story 18.4's measurement closes whether 18.4 actually closes the gap (per architecture OFR-E: "ratio < 1.0× sustained on RTX 5090"). If yes, the memory entry is **MARKED CLOSED** with the post-Story-18.4 ratio; if no (sub-20% speedup → OQ #1 routing), the memory entry stays open with the new diagnosis (compile didn't fix it either; the producer bottleneck is likely deeper in the talker's autoregressive loop)

## Tasks / Subtasks

- [x] **Task 1 — D-22 verification + pin-bump landing** (AC: #1, #2)
  Re-verify the 2026-05-10 research subagent's finding empirically; bump the pin; bump the constant; update the internals test. **First-AC obligation** per architecture's "First implementation priority" at line 1479 of `architecture-streaming-acceleration-and-lightning-tier.md`.
  - [x] 1.1 Re-run the verification grep on the **current** `python310/Lib/site-packages/qwen_tts/` tree to confirm `enable_streaming_optimizations` is still absent at the existing pin. Capture the verbatim grep transcript at evidence file §"D-22 verification (Branch A vs Branch B)". Done: zero matches; Branch B fires.
  - [x] 1.2 Read the introducing fork's diff at `https://github.com/dffdeeq/Qwen3-TTS-streaming/commit/3fdb4682` (3 files; +50/-6 lines). Diff is purely additive; full SHA `3fdb468233d73fa537202b94a1cc7c4e7a6160b8`; pyproject declares `name = "qwen-tts", version = "0.0.4"` — drop-in replacement. Captured at evidence §"Pin-bump rationale".
  - [x] 1.3 Update `requirements.txt:23` to `qwen-tts @ git+https://github.com/dffdeeq/Qwen3-TTS-streaming.git@3fdb468233d73fa537202b94a1cc7c4e7a6160b8`
  - [x] 1.4 Update `build_tools/requirements-production.txt:61` identically
  - [x] 1.5 Update `qwen_tts_service.py:1150` `_QWEN_TTS_PIN_HASH = "3fdb4682"`
  - [x] 1.6 Update `tests/test_qwen_tts_internals.py:48-:60` (`expected_methods` extended with `enable_streaming_optimizations`)
  - [x] 1.7 `pip install --upgrade --force-reinstall --no-deps "git+https://github.com/dffdeeq/Qwen3-TTS-streaming.git@3fdb468233d73fa537202b94a1cc7c4e7a6160b8"` succeeded; import attribute check OK. Captured at evidence.
  - [x] 1.8 `pytest tests/test_qwen_tts_internals.py -v` → 9 passed in 5.68s (5 pre-existing + 4 Story 16.8 + new `enable_streaming_optimizations` row).
  - [x] 1.9 Focused regression smoke (`tests/unit/services/tts_streaming/`, `tests/unit/services/test_model_registry.py`, `tests/unit/models/`) → 211 passed in 9.30s. Zero regressions on bumped pin in core unit-test surface. Broader 803+ sweep is Task 11.

- [x] **Task 2 — `compile_cache.py` module** (AC: #3 + architecture P-10)
  Create the single-helper compile-cache module. Pure-decision discipline; mirrors Story 18.2 / 18.3's lazy-import + side-effect-free patterns.
  - [x] 2.1 Created `src/myvoice/services/tts_streaming/compile_cache.py` with the canonical docstring citing D-23, D-24, D-25, P-10, Story 17.2 H1/H2.
  - [x] 2.2 `compute_key(*, qwen_tts_pin_hash, model_id, precision_str, torch_version, decode_window_frames, cuda_capability, compile_mode) -> str` — SHA-256 hexdigest of `"|"`-joined dimensions; side-effect-free.
  - [x] 2.3 `cache_root() -> Path` — `%LOCALAPPDATA%/MyVoice/torch_compile_cache` (Windows) / `~/.cache/MyVoice/torch_compile_cache` (non-Windows); idempotent mkdir.
  - [x] 2.4 `cache_dir_for_key(key: str) -> Path` — `cache_root() / key[:16]`; idempotent mkdir.
  - [x] 2.5 `is_warm(key: str) -> bool` — meta.json exists + full-key match; defensive against unparseable / wrong-key meta.
  - [x] 2.6 `mark_warm(key: str) -> None` — writes meta.json with `{key, warmed_at, torch_compile_cache_dir}`.
  - [x] 2.7 `set_torchinductor_cache_dir(key: str) -> None` — sets env var; INFO log once per process; idempotent.
  - [x] 2.8 `tts_streaming/__init__.py` re-exports the `compile_cache` module + adds to `__all__`.
  - [x] 2.9 `tests/unit/services/tts_streaming/test_compile_cache.py` — 16 rows covering the seven AC #3 sub-bullets (stability across invocations + across processes; uniqueness parametrized one-per-dimension; Windows + non-Windows roots; round-trip; collision defense; unparseable-meta defense; set_torchinductor idempotency). All 16 pass.

- [x] **Task 3 — `engage_compile_optimizations()` in `torch_runtime.py`** (AC: #4 + architecture D-21, D-22 Branch A invariant, D-25, P-12)
  Wire the upstream API into the model-load path. Hardware-gated, NFR7-fallback-aware, capability-probed, telemetry-emitting.
  - [x] 3.1 Added `engage_compile_optimizations(model, *, app_settings=None, streamer_chunk_size=25, streamer_lookahead=5) -> dict` to `torch_runtime.py`.
  - [x] 3.2 Eight reason branches implemented (`cuda_unavailable`, `pre_ampere`, `user_disabled`, `engaged_warm_cache`, `engaged_cold_compile`, `compile_failed`, `probe_failed`, `pre_ampere_user_explicit`). Lazy imports of torch + compile_cache.
  - [x] 3.3 D-25 decode-window invariant: `assert decode_window_frames == streamer_chunk_size + streamer_lookahead`. Default 30; parameters allow future streamer re-tune.
  - [x] 3.4 D-22 Branch A invariant assertion: `assert callable(getattr(model, "enable_streaming_optimizations", None))`.
  - [x] 3.5 Cache-key wiring via `compile_cache.compute_key(...)` + `set_torchinductor_cache_dir(key)` before API call.
  - [x] 3.6 API call wrapped in `try/except (TypeError, RuntimeError, AttributeError, Exception)`; eager fallback returns `compile_failed` with error tag.
  - [x] 3.7 P-12 probe via `_probe_compile_engaged(model)` — checks `_torchdynamo_orig_callable` / `__wrapped__` / `_orig_mod` on `model.model.talker.model.forward`. Probe choice documented at evidence §"P-12 capability probe selection". Selected sentinel: `_torchdynamo_orig_callable` (PyTorch 2.x canonical).
  - [x] 3.8 Telemetry: one `metrics.record("tts_compile_engaged", ...)` per branch; tags = `reason`, `decode_window_frames`, `cuda_capability`, `cache_warm`, `error` (compile_failed only).
  - [x] 3.9 INFO log line `"torch.compile + CUDA Graph engaged (decode_window_frames=%d, cuda_capability=%s, cache=%s)"` on success; per-branch skip messages on non-engaged.
  - [x] 3.10 `tts_streaming/__init__.py` re-exports `engage_compile_optimizations` + `compile_cache` module.
  - [x] 3.11 `test_torch_runtime.py` extended with 8 `reason`-enum rows + `_isolated_inductor_env` fixture + `_PlainObj` helper for talker chain (avoids MagicMock auto-attribute issue). All 8 rows pass.
  - [x] 3.12 Updated `test_codec_token_streamer.py::test_package_all_lists_expected_symbols_in_order` and `test_streaming_decoder.py::test_package_all_lists_expected_symbols_in_declaration_order` to append `engage_compile_optimizations` + `compile_cache`. Both pass.

- [x] **Task 4 — `ModelRegistry` wire-up + INFO log extension** (AC: #5)
  Call `engage_compile_optimizations` from the model-load path; extend the existing INFO log with `compile_engaged='{state}'`.
  - [x] 4.1 Added `self._app_settings = app_settings` storage in `ModelRegistry.__init__`; added `engage_compile_optimizations(model, app_settings=self._app_settings)` invocation in `_load_model_sync` after `Qwen3TTSModel.from_pretrained` returns (and after the existing optional `MYVOICE_DTYPE_AUDIT` instrumentation). The call is wrapped in defensive try/except — engage is expected to absorb its own exceptions per NFR7, but the catch defends against probe-path exceptions.
  - [x] 4.2 Extended the existing __init__ INFO log with `compile_engaged='deferred'` (the engagement happens at load time, not init time). Added a NEW post-load INFO log line `"ModelRegistry model loaded: model_type=..., compile_engaged='{state}', compile_reason='{reason}'"` after `engage_compile_optimizations` returns — this is the canonical observability for the resolved compile state. The append-only widening preserves log-parser backward-compat.
  - [x] 4.3 SKIPPED per Task 4.3 "Optional refinement" guidance — the new `tts_compile_engaged` metric carries the compile state independently; mixing it into `tts_precision_resolved` would couple the two stories' tag schemas (M5 fragility class).
  - [x] 4.4 `tests/unit/services/test_model_registry.py` extended with two new rows: `test_app_settings_stored_on_self_for_load_model_compile_engagement` + `test_modelregistry_init_info_log_includes_compile_engaged_deferred`. Plus `_StubAppSettings` widened to accept `tts_compile`. 16/16 pass (14 pre-existing + 2 new).

- [x] **Task 5 — `AppSettings.tts_compile` field** (AC: #6)
  Data-only field; mirrors `tts_precision` (Story 18.3) / `streaming_mode_override` (Story 16.6) patterns exactly. No UI surface.
  - [x] 5.1 Added `tts_compile: str = "auto"` declaration at `app_settings.py` immediately after `tts_precision`.
  - [x] 5.2 Added `__post_init__` validation block (`valid_tts_compile_values = ["auto", "on", "off"]`) immediately after `tts_precision`'s block; emits `UNKNOWN_TTS_COMPILE` warning and resets to `"auto"`.
  - [x] 5.3 Added `"tts_compile": self.tts_compile` in `to_dict`; added `tts_compile=data.get("tts_compile", "auto")` in `from_dict`.
  - [x] 5.4 Added `"tts_compile"` to the `reset_to_defaults()` field-name list immediately after `"tts_precision"`.
  - [x] 5.5 Created `tests/unit/models/test_app_settings_tts_compile.py` with 11 rows mirroring `test_app_settings_tts_precision.py` verbatim — five categories (default / accepted values / round-trip / validation / reset). All 11 pass.

- [x] **Task 6 — `QwenTTSService.warmup_compile_async` + orchestrator wire-up** (AC: #7)
  Background warmup worker; "Preparing TTS engine…" indicator; persistent-cache hit/miss logic.
  - [x] 6.1 Added `warmup_compile_async(self) -> None` to `QwenTTSService` immediately after `_emit_preparing_voice`. Plus a helper `_run_compile_priming` for unit-test mockability.
  - [x] 6.2 Cache-hit path: computes key via `compile_cache.compute_key(...)`, calls `compile_cache.is_warm(key)`. On hit → INFO log "Compile cache hit; skipping warmup priming" + telemetry `cache_hit` + return.
  - [x] 6.3 Cache-miss path: `_emit_preparing_voice("Preparing TTS engine…")` → `await self._run_compile_priming()` (dispatches `generate_custom_voice("Hello world.", speaker="Ryan")` through the production TRUE_STREAM chain) → `compile_cache.mark_warm(key)` → `_emit_preparing_voice(None)` → INFO log + telemetry `primed_cold`.
  - [x] 6.4 Priming-failure path: try/except RuntimeError + any Exception → WARNING log, indicator clears via `finally`, `mark_warm` NOT called, telemetry `priming_failed` with error tag.
  - [x] 6.5 Lazy-fallback env-var gate: `MYVOICE_DISABLE_COMPILE_WARMUP=1` → return early with telemetry `user_disabled`.
  - [x] 6.6 Hardware-gated skip: `is_ampere_or_newer()` False → return early with telemetry `pre_ampere` or `cuda_unavailable`.
  - [x] 6.7 `app.py` orchestrator wired the fire-and-forget task after `hydrate_voice_clone_prompt_cache` via `self._run_async_task(self._tts_service.warmup_compile_async(), ...)`. Outer try/except mirrors Story 17.2's hydration discipline. Story 18.3 H1 static-scan tests still pass (3/3).
  - [x] 6.8 Created `tests/unit/services/test_qwen_tts_service_compile_warmup.py` with 6 rows (5 AC-required + 1 defensive `no_model_loaded`). Monkeypatches `is_ampere_or_newer`, `compile_cache.is_warm` / `mark_warm`, `_run_compile_priming`, and the env var. All 6 pass.

- [ ] **Task 7 — Bundled-environment smoke** (AC: #8)
  Composes Story 18.3's bundled smoke + the new pin-bump + the new compile wire-up. First production-bundle verification of the combined Epic 18 work (18.2 + 18.3 + 18.4).
  - [ ] 7.1 Run `build_release.bat` (per `memory/build_tools_phase_perp_state.md` + `memory/production_release_state.md`). Confirm the resulting `build_tools/dist/MyVoice/MyVoice.exe` includes the bumped pin (verify by inspecting `_internal/qwen_tts/inference/qwen3_tts_model.py` for the `enable_streaming_optimizations` symbol). [COMMANDER-ROUTED]
  - [ ] 7.2 Launch the bundled exe. Confirm `myvoice.log` (in the portable Logs path) contains the expected INFO lines per AC #8 (TF32, ModelRegistry-extended-with-compile, torch.compile-engaged, Compile-cache-cold-or-hit). Capture all four verbatim at evidence file §"Bundled smoke (initial run)"
  - [ ] 7.3 Run a short canonical TTS generation (Sarira-F short paragraph). Confirm no new error / warning / unexpected log line; confirm TTS generation completes successfully end-to-end
  - [ ] 7.4 Run a long-form CLONED utterance (Sarira-F, ≥250 chars / ~22 s). Commander confirms zero perceptual defects compared to the pre-Story-18.4 baseline (Commander-solo spot-check; independent of the multi-listener audition gating the architectural amendment)
  - [ ] 7.5 Re-launch the bundled exe (warm-cache run). Confirm the `cache=warm` log line replaces `cache=cold` and the warmup-priming line shows `cache_hit`. Capture at evidence file §"Bundled smoke (warm-cache run)"
  - [ ] 7.6 If the bundled smoke surfaces a packaging defect (e.g., the new `compile_cache.py` fails to import in PyInstaller for a hidden-imports reason; the bumped fork pin's PyInstaller signature differs from the upstream pin; the inductor cache directory creation fails for a permissions reason), surface to Commander rather than absorbing it. Document at evidence file §"Bundled smoke" with the defect's exact log excerpt + the proposed fix surface (typically a one-line `hidden-imports` entry in the PyInstaller spec; or a runtime-fallback path for the cache directory)

- [x] **Task 8 — NFR1 first-chunk-latency 3-way A/B/C empirical measurement harness** (AC: #9) [HARNESS COMPLETE; MEASUREMENT RUNS COMMANDER-ROUTED]
  Three branches, N=10 per branch; producer-bottleneck ratio computation; cold-vs-warm distinction; OQ #1 routing on sub-20% speedup. Composes Story 18.3's harness pattern.
  - [x] 8.1 Created `05_Story_18.4_NFR1_BF16_COMPILE.bat` + `06_Story_18.4_NFR1_BF16_EAGER.bat` + `07_Story_18.4_NFR1_FP32_EAGER.bat` at repo root mirroring Story 18.3's batch-file discipline. Each uses `MYVOICE_AUTO_QUIT_ON_CLOSE=1` for the N=10 fresh-process loop.
  - [x] 8.2 Created `_bmad-output/implementation-artifacts/18-4-set-precision-and-compile.py` toggling BOTH `tts_precision` AND `tts_compile` in `settings.json`. Verified end-to-end with `bf16/on` and `auto/auto` round-trip.
  - [x] 8.3 Created `_bmad-output/implementation-artifacts/18-4-aggregate-nfr1.py` — 3-way aggregator (A vs B compile gain; A vs C compounded gain; B vs C bf16 re-validation) with median+p90+p95 per branch, producer steady-state ratio per branch (Story 18.1 §4.4 methodology), OFR-E gate check, and Task 8.6 OQ #1 routing surface. Syntax-validated.
  - [x] 8.4 Cold-compile discipline encoded in aggregator: branch A's run #1 is discarded from the median; reported separately at evidence file §"NFR1 first-chunk-latency measurement (3-way A/B/C)". [COMMANDER runs the actual N=10 loops]
  - [ ] 8.5 [PENDING COMMANDER] Compute deltas after Commander runs the three .bat files; the aggregator script surfaces all numbers verbatim. Capture output at evidence file §"NFR1 first-chunk-latency measurement (3-way A/B/C)".
  - [ ] 8.6 [PENDING COMMANDER] OQ #1 routing condition automatically surfaces in aggregator output: sub-20% A-vs-B speedup OR branch-A ratio ≥ 1.0× sustained → STOP and route to OQ #1.

- [x] **Task 9 — NFR3 joint audition** (AC: #10) [FULL PASS 2026-05-11]
  Single-cycle audition certifies bf16 + compile + pin-bump simultaneously.
  - [x] 9.1 Created `_bmad-output/implementation-artifacts/18-4-l1-audition-helper.py` pointing at `18-4-perceptual-fixtures/`. Verdict-gate reminder updated for Story 18.4 framing ("PASS iff zero `audible_seam` flags on B (bf16+compile+new-pin) trials across all 30 trials"). Syntax-validated.
  - [x] 9.2 Fixture regen — **AUTOMATED via `18-4-regen-fixture.py`** (drives `Qwen3TTSModel.generate_voice_clone` directly with the Sarira-F voice_clone_prompt cache; bypasses the GUI). Both branches generated in 2 min flat; 20 WAVs at PCM_16 24 kHz mono; compile-engaged Branch B showed ~2× speedup on long utterances (l-013/l-014). Force-added per `memory/git_repo_state.md`. Truth-table built via `18-4-generate-truthtable.py` with deterministic per-listener randomization (L1: 5/5, L2: 4/6, L3: 5/5 fp32-as-trial-A counts).
  - [x] 9.3 All 3 listeners recruited: L1 = Commander; L2 + L3 = co-located in-person walkthrough listeners per Story 17.1 protocol. Sessions ran 2026-05-11 via `08_Story_18.4_NFR3_Audition.bat` (mirrors `01_Run_MyVoice_With_CSV_Capture.bat` structure).
  - [x] 9.4 All 3 sessions completed cleanly (10/10 rows each = 30 total rows). Capture at `18-4-bf16-compile-pinbump-audition.csv`.
  - [x] 9.5 **Full audition verdict: PASS.** `18-4-compute-verdict.py` cross-references truth-table. Per-actual-mode defect counts: `none` = 30/30 on both fp32_eager and bf16_compile; ALL other defect categories = 0 across all 60 observations. Zero `audible_seam` flags (verdict gate). Per-listener actual-mode preferences: L1 = 1 bf16_compile (s-015, "better quality/volume") + 9 equivalent; L2 = 10 equivalent; L3 = 10 equivalent. Total: 1/30 trials prefers bf16_compile, 29/30 equivalent, 0/30 prefers fp32_eager.
  - [x] 9.6 N/A — verdict is PASS, no OQ #2 routing.

- [x] **Task 10 — Architecture amendment** (AC: #11) [FULL FORM, 2026-05-11]
  Mirrors the Story 17.1 / Story 18.3 two-place edit pattern + the sealed-architecture amendment marking D-22 Branch B as EXECUTED. Landed in **L1-partial form** 2026-05-11, **re-amended to FULL PASS form** the same day after L2 + L3 audition completed cleanly.
  - [x] 10.1 Place 1: appended the new Story 18.4 parenthetical to the NFR3 cell at `architecture-optimization-pass.md:803` after Story 17.1 + Story 18.3 parentheticals (cumulative pattern). Records: certified bf16 + compile + pin-bump; OFR-E close (3.23× → 0.670×); **FULL PASS** — zero `audible_seam` flags across all 30 trials × 2 conditions = 60 defect observations from 3 listeners; one slight bf16_compile preference (L1 on s-015), 29 equivalent.
  - [x] 10.2 Place 2: added new section `#### Story 18.4 Follow-up Note (Joint bf16 + Compile + Pin-Bump Audition — FULL PASS, 2026-05-11)` immediately after the existing `#### Story 18.3 Follow-up Note` block. Section covers: D-22 verification + Branch B firing rationale; pin-bump rationale (compile_talker=False; cache invalidation via _QWEN_TTS_PIN_HASH constant bump; quarterly upstream check); fixture regeneration methodology (automated via `18-4-regen-fixture.py`); NFR1 3-way A/B/C measurement table + producer-bottleneck ratio history (3.23 → 1.40 → 1.62 → 0.670); split-verdict mechanism + OQ #1 override; NFR3 audition **FULL PASS** defect-flag table + per-listener actual-mode preferences; architectural decision (ship + compile-as-default-on-Ampere+ once Story 18.5 unblocks the bundle); NFR3 fully re-cleared; methodology composition; three methodology limitations.
  - [x] 10.3 Source artifacts list at the new section's footer mirrors Story 17.1 / 18.3's footer: ✓ for git-tracked (story file, evidence file, 30 NFR1 CSVs, 3 consolidated CSVs, 20 fixture WAVs, truth-table, audition CSV with 30 rows, 7 helper scripts, 4 .bat harnesses); ○ N/A (everything force-added per `memory/git_repo_state.md`).
  - [x] 10.4 Amended `_bmad-output/planning-artifacts/architecture-streaming-acceleration-and-lightning-tier.md` D-22 entry with `**EXECUTED 2026-05-11 per Story 18.4 — Branch B fired**` marker + FULL PASS verdict. Preserves the Branch A vs Branch B design-time framing; adds the empirical resolution + pin-bump SHA + 60-observation audition outcome + cross-reference to the follow-up note.

  **Re-amend executed 2026-05-11.** L2 + L3 audition rows landed; verdict flipped from L1-PARTIAL-PASS to FULL PASS via in-place edits to Places 1 + 2 + 3 (same parentheticals + follow-up note sections, not a new amendment cycle). NFR3 fully re-cleared for the bf16+compile+new-pin composite.

- [x] **Task 11 — Regression test sweep** (AC: #12)
  Verify the bumped pin + new compile module + new AppSettings field + ModelRegistry wire-up + warmup worker do not regress the established surfaces. Mirrors Story 18.2 Task 7 / Story 18.3 Task 11 structure.
  - [x] 11.1 New + extended unit tests pass: 79/79 (35 pre-existing test_torch_runtime rows + 8 compile-engage rows; 16 test_compile_cache rows; 14 pre-existing test_model_registry + 2 new rows; 11 test_app_settings_tts_compile rows; 6 warmup rows; 9 qwen_tts_internals rows including the new `enable_streaming_optimizations` row).
  - [x] 11.2 Story 18.1 instrumentation tests pass (part of full unit/services run).
  - [x] 11.3 Story 17.3 progressive-playback tests pass (part of full unit run, including `tests/unit/test_app_progressive_playback.py` + the integration `test_progressive_playback_dispatch_skip.py`).
  - [x] 11.4 Story 16.2 streaming-mode tests pass (part of `tests/unit/services/tts_streaming/test_streaming_mode.py`).
  - [x] 11.5 Story 17.2 voice_clone_prompt cache tests: 31/31 pass — `test_pin_mismatch_invalidates_pt` already covers the pin-mismatch invalidation path and PASSES against the new constant `3fdb4682` (the existing test constructs a `.pt.meta.json` with a non-matching pin and asserts the cache rejects it; Story 18.4's pin bump exercises this path automatically since the new constant differs from the previous one). NO additional regression test needed — Story 17.2's discipline already mirrors the exact bug class.
  - [x] 11.6 Broader Epic 16/17/18 regression sweep: **857/857 PASS** on the Story 18.4-relevant scope (`tests/unit/services/` + `tests/unit/observability/` + `tests/unit/models/` + `tests/unit/test_app_qwen_tts_construction.py` + `tests/unit/test_app_progressive_playback.py` + `tests/integration/test_progressive_playback_dispatch_skip.py` + `tests/test_qwen_tts_internals.py`). Story 18.3 closed at 803; Story 18.4 adds ~50 new rows (16 compile_cache + 8 engage_compile + 11 tts_compile + 6 warmup + 2 model_registry + 1 internals + minor) for a 857 total — well above the architectural expectation of 825+.
  - [x] 11.7 Updated `__all__`-ordering pinned tests in `test_codec_token_streamer.py` and `test_streaming_decoder.py` to append `engage_compile_optimizations` + `compile_cache`. Both pass.
  - [x] 11.8 No regressions in the Story 17.2 voice-clone tests against the new pin → no OQ #3 routing trigger. Pre-existing failures in `tests/unit/ui/dialogs/voice_design_studio/` (30 failed + 3 errors) are entirely outside Story 18.4's scope — Story 18.4 did NOT touch any UI code; the failures are UI tab-count / audio-player-widget assertions unrelated to the pin bump or compile work. Documented at evidence file §"Out-of-scope but tracked" as pre-existing-working-tree state.

- [x] **Task 12 — Memory updates + close-out** (AC: #13)
  Memory pointers, PRD back-prop tracking, build-counter follow-up.
  - [x] 12.1 Updated `memory/epic18_producer_bottleneck_finding.md` with a chronological status section noting Story 18.4's source-tree-edit landing + the OFR-E target + the Commander-routed dependency on Task 8's measurement for the actual CLOSE/OPEN decision. Memory entry stays OPEN until Commander runs the 3-way A/B/C measurement.
  - [x] 12.2 Updated `memory/build_tools_phase_perp_state.md` with the pin-bump to `dffdeeq/Qwen3-TTS-streaming@3fdb468233d73fa537202b94a1cc7c4e7a6160b8` + the upstream-check follow-up + the build-time pin-verification SHA-256 hashes that need updating before Task 7's bundled smoke. The previous HIGH "TRUE_STREAM voice_clone_prompt regression" follow-up was already resolved by Story 17.2; the pin-bump invalidates Story 17.2's cached `.pt` files cleanly on first run (verified by `test_pin_mismatch_invalidates_pt` continuing to pass against the new constant).
  - [x] 12.3 PRD back-propagation of OFR-E documented as out-of-scope at evidence file §"Out-of-scope but tracked" (Owner = PM/Commander per architecture line 1554; not blocking Story 18.4).
  - [x] 12.4 Build-counter increments (`12 → 13`) noted as Commander-handled in a separate build-state commit per Story 18.2 / 18.3 OQ #4 precedent. Documented at evidence file §"Out-of-scope but tracked".
  - [x] 12.5 Story status flow: `ready-for-dev → in-progress` at task-start (sprint-status.yaml updated). After all dev-agent-autonomous tasks (1, 2, 3, 4, 5, 6, 8 harness, 9 helper, 11 regression) close, status flips to `review`. Tasks 7 (bundled smoke), 8 measurement runs, 9 fixture+audition, 10 architecture amendment (contingent on 9), and 13 code-review pass remain Commander-routed (final flip to `done` waits on those).

- [x] **Task 13 — Code-review pass** (post-implementation)
  Per Stories 16.7 / 16.8 / 17.1 / 17.2 / 17.3 / 18.1 / 18.2 / 18.3 precedent.
  - [x] 13.1 Run `/bmad-bmm-code-review`. Per `memory/code_review_regression_test_exact_class.md`: HIGH/MEDIUM-fix regression tests must mirror the exact bug class. Anticipated review-finding categories for this story (tag in advance so the review's discovery surface is the story's actual gaps, not these expected ones):
    - (a) **D-22 verification drift** — the empirical re-verification at Task 1.1 must produce the same Branch B verdict the architecture frames; if Branch A fires (the pin DOES have the API now, e.g., upstream picked up the patch in the meantime), the story splits and Tasks 1.3 / 1.4 / 1.5 / 1.6 short-circuit
    - (b) **Cache-key drift** (P-10 enforcement) — the 7-dim key must be constructed in EXACTLY one place (`compile_cache.compute_key`); any inline construction at the call site is a P-10 violation
    - (c) **D-25 invariant assertion drift** — the decode-window invariant must be a HARD `AssertionError` not a `log.warning(...)` (silent fallthrough is the failure mode this AC explicitly forbids per architecture P-11)
    - (d) **NFR7 fallback completeness** — every failure path (compile_failed, probe_failed, hardware_unavailable, user_disabled) must drop cleanly back to eager mode without breaking the dispatch chain. The 8-branch test row coverage at AC #11 is the canonical regression surface
    - (e) **Telemetry tag schema drift** — `source` enum values + `device_capability` `"none"` sentinel must match Stories 18.2 / 18.3's contract verbatim
    - (f) **`_QWEN_TTS_PIN_HASH` constant bump completeness** — the pin-bump must invalidate Story 17.2's cached `.pt` files cleanly. The Task 11.5 regression test is the canonical pin-mismatch coverage
    - (g) **Audition fixture regeneration discipline** — the regenerated `18-4-perceptual-fixtures/` MUST NOT overwrite or rename `16-7-perceptual-fixtures/` or `18-3-perceptual-fixtures/` (those preserve Story 17.1 / 18.3's audited verdicts' evidentiary surface)
    - (h) **Architecture-amendment placement drift** — the new follow-up note must land **after** Story 18.3's follow-up note (chronological ordering preserves the document's reading flow)
    - (i) **Bundled-smoke evidence file completeness** — the bundled-smoke section must include both cold-cache and warm-cache run captures (the warm-cache cache=warm log line is the canonical observability signal for "the persistent cache works")
  - [x] 13.2 Address findings. Re-run code-review twice after non-trivial auto-fixes per the established Stories 16.7 / 16.8 / 17.1 / 17.2 / 17.3 / 18.1 / 18.2 / 18.3 pattern. Commit per the `Story 18.4: code-review pass — H#/M#/L# fixes` pattern.

    **Code-review pass executed 2026-05-11 (Opus 4.7 [1m]).** Surfaced 3 HIGH + 4 MEDIUM + 3 LOW; auto-fixed H1/H2/H3 + M1/M2/M4 (3 LOW deferred as docs-only / non-load-bearing). Second-pass re-audit confirmed no new regressions. Findings + fixes:
    * **H1 — `warmup_compile_async` ignored `tts_compile="off"`.** With the Fix #4 default flipped to "off", an Ampere+ first-launch would fire `_run_compile_priming` → audible "Hello world." reaching the wired audio_chunk_ready_callback + a meaningless meta.json sidecar written despite engage staying eager. Fix at `qwen_tts_service.py:1843-1865`: new Gate 1b reads `getattr(self._app_settings, "tts_compile", None)`; on "off", short-circuits with reason=`user_disabled` (matches engage vocabulary). Regression test at `test_qwen_tts_service_compile_warmup.py::test_warmup_tts_compile_off_skips_priming_and_gate_log` mirrors the exact bug class — asserts is_warm/mark_warm/_run_compile_priming none called + indicator never fires.
    * **H2 — D-25 invariant assertion was tautological (dead code).** Previous `decode_window_frames = streamer_chunk_size + streamer_lookahead; assert decode_window_frames == streamer_chunk_size + streamer_lookahead` always passed. Fix at `torch_runtime.py:519-540`: `engage_compile_optimizations` now accepts `decode_window_frames: Optional[int] = None`; `None` → derive; explicit → `assert == streamer_window` hard-fail per P-11 "loud at startup". Regression test at `test_torch_runtime.py::test_engage_compile_d25_invariant_raises_on_explicit_window_mismatch` exercises raise (`decode_window_frames=80`) + happy paths (None + matching 30).
    * **H3 — Validator reset target diverged from field default.** `UNKNOWN_TTS_COMPILE` reset to `"auto"` but the field's declared default is `"off"` — a user with typo silently re-engaged compile on the broken Windows triton toolchain. Fix at `app_settings.py:438-466`: validator reset target → `"off"` matching declaration. Test `test_invalid_value_resets_to_off_in_post_init` updated; `test_invalid_value_emits_unknown_warning_code` asserts "off" reset.
    * **M1 — `torch_runtime.py` imported `QwenTTSService` to read pin hash.** Violated tts_streaming module-boundary discipline. Fix: `engage_compile_optimizations` accepts `qwen_tts_pin_hash: Optional[str] = None` parameter; `ModelRegistry._load_model_sync` reads the constant and passes through (the caller is the correct boundary layer). `torch_runtime.py` no longer imports `services.qwen_tts_service`.
    * **M2 — `_run_compile_priming` docstring claimed Sarira-F + `generate_voice_design`.** Implementation uses Ryan + `generate_custom_voice`. Fix: docstring updated to match the canonical default speaker and the actual dispatch path.
    * **M4 — `except (TypeError, RuntimeError, AttributeError, Exception)` redundant tuple.** `Exception` makes the prior types redundant. Fix at `torch_runtime.py`: `except Exception as exc:` with `# noqa: BLE001 — NFR7 graceful degradation` justification.

    **Deferred LOW findings** (non-load-bearing; tracked for future iteration):
    * L1 — `mark_warm` not strictly idempotent (rewrites timestamp on each call); harmless given the warmup runs once per process startup.
    * L2 — P-12 probe's third clause checks `predictor_inner._orig_mod` (module-wrap sentinel) rather than on `fwd`; first two clauses are load-bearing, third is defensive future-proofing.
    * L3 — Build counter at `installer.iss`/`version.py` is `15` not `13`; per the project's pattern these edits land in the Commander-routed build-state commit, so the value already reflects the four bundled-smoke rebuild attempts of Fix #1–#4.

    Test surface post-fix: 88/88 PASS across `test_torch_runtime` + `test_compile_cache` + `test_qwen_tts_service_compile_warmup` + `test_app_settings_tts_compile` + `test_model_registry` (one new H1 test row + one new H2 test row + two tts_compile validator-reset rows updated; signature changes to engage propagated through warm-cache test).

## Dev Notes

### What this story is

Story 18.4 is the fourth and final story of Epic 18 (Generation-Speed Optimizations / Phase ⊥-Polish-2). It executes architecture decision **D-22 Branch B** (qwen-tts pin-bump from `QwenLM/Qwen3-TTS@1ab0dd75` to `dffdeeq/Qwen3-TTS-streaming@3fdb4682`, the introducing commit of the upstream `enable_streaming_optimizations()` API), wires that API into the model-load path with a 7-dim persistent compile cache (architecture D-24), enforces the decode-window invariant (D-25) loudly at startup (P-11), wires a background warmup worker that primes the inductor cache on cold first run + reads from the warm cache on subsequent runs (D-23), and runs a single joint NFR3 ≥3-listener perceptual A/B audition that simultaneously certifies three dimensions: bf16 (deferred from Story 18.3), compile (this story), and the new pin (D-22 Branch B's re-audition obligation per `architecture-optimization-pass.md:803`).

**Architecture binds the technical choices.** Per `architecture-streaming-acceleration-and-lightning-tier.md` (sealed 2026-05-10):

- **D-21:** `decode_window_frames=30` fixed (matches MyVoice's `chunk_size=25 + lookahead=5`); `dynamic=True` (the original epic-stub design at `epics-optimization-pass.md:1395`) is **rejected** because it forfeits CUDA Graph replay (most of the speedup). Story 17.1's audition cleared the 30-window's perceptual quality, so this choice preserves that verdict.
- **D-22 Branch B:** verified empirically by the 2026-05-10 research subagent + re-verified at Task 1.1 by the dev agent. Pin-bumps `requirements.txt` + `requirements-production.txt` + the `_QWEN_TTS_PIN_HASH` constant.
- **D-23:** background warmup at app start + persistent compile cache at `%LOCALAPPDATA%/MyVoice/torch_compile_cache/`. Cold-compile UX is a "Preparing TTS engine…" indicator that mirrors Story 17.2's "Preparing voice…" pattern. Lazy fallback path (env-var-gated) preserves test surface.
- **D-24:** 7-dim cache key (qwen_tts_pin_hash, model_id, precision_str, torch_version, decode_window_frames, cuda_capability, compile_mode) constructed via `compile_cache.compute_key()`. P-10 enforces the single-helper discipline.
- **D-25:** decode-window invariant assertion at startup; `AssertionError` on mismatch (not `log.warning(...)` — that's the silent-fallthrough anti-pattern P-11 explicitly catalogues).

**The audition is joint** per the user's `/bmad-bmm-create-story` direction 2026-05-10 + architecture D-27.3. A single ≥3-listener cycle certifies bf16 + compile + pin-bump simultaneously; the fixture is regenerated under the new pin + bf16 + compile-engaged production target. PASS gate is verbatim Story 17.1's: zero `audible_seam` flags across all 30 trials.

**This is the load-bearing producer-side fix per `memory/epic18_producer_bottleneck_finding.md`.** Story 18.1 measured the producer ratio at 3.23× (talker @ 31% real-time); Story 18.2's TF32+cuDNN closed it to 1.40 on fp32-eager; Story 18.3's bf16 closed nothing (-3.77% measured). Story 18.4's compile-engaged target ratio is **<1.0× sustained on RTX 5090** per architecture OFR-E. If 18.4 also produces no measurable speedup, that's diagnostic insight that the producer bottleneck is deeper in the talker's autoregressive loop (HF's `generate()` internal path that neither 18.2/18.3/18.4 reaches directly), and the epic retros with that finding routed via OQ #1.

### What this story is NOT

- **NOT a `dynamic=True` `torch.compile` wrap.** The original epic stub at `:1395` named `dynamic=True`; the architecture rejects this (D-21) because dynamic forfeits CUDA Graph replay. The fixed-window choice is the architectural ground truth; the streamer's existing `chunk_size + lookahead = 30` is the design target.
- **NOT a MyVoice-side `torch.compile` wrap of `speech_tokenizer.decode`.** The architecture binds D-22 Branch B (use the upstream API); the fork's `enable_streaming_optimizations()` does the wrap correctly + applies it to the codebook predictor (where Story 18.1's bottleneck lives) + handles CUDA Graph capture. MyVoice's job is to call the API, not reimplement it.
- **NOT a `stream_generate_pcm()` adoption.** The fork's new streaming entrypoint replaces the existing dispatch chain; Story 18.4 stays on the existing Story 16.8 forward-hook + CodecTokenStreamer + StreamingDecoderWorker pipeline. The dispatch chain's audited correctness is preserved; only `enable_streaming_optimizations()` is wired in.
- **NOT a UI surface for the `tts_compile` setting.** Data-only field; UI exposure deferred (hand-edit `settings.json` is the user remediation path, mirroring Story 18.3's `tts_precision` precedent).
- **NOT a CPU / pre-Ampere change.** Per D-9 / NFR12, CPU and pre-Ampere hosts skip compile entirely (`reason="cuda_unavailable"` or `"pre_ampere"` telemetry breadcrumb). The `tts_compile = "on"` user-explicit override does NOT escape the hardware gate (CPU bf16 emulation precedent — emulation slows things down; compile would too).
- **NOT an `audible_seam` gate-relaxation.** The verdict gate is verbatim Story 17.1's: PASS iff zero listeners flag `audible_seam`. The dev agent does NOT widen the gate to permit "low-amplitude" defects or "minor sibilance"; FAIL routes to Open Question #2.
- **NOT a streamer re-tune to `decode_window_frames=80`.** The architecture's D-21 considered three sub-options (a) align API to 30 [chosen]; (b) refactor streamer to 80 + full re-audition; (c) keep dynamic. Option (a) wins because the 30 value was validated by Story 17.1 audition. Story 18.4 does NOT touch the streamer's chunk_size or lookahead.
- **NOT a flash-attn pin / runtime verification.** That's architecture D-26 / Story 18.5 (separate story per architecture D-38 phased migration). Story 18.4 touches only the compile dimension.
- **NOT a two-phase emission scheduler.** That's architecture D-28 / Story 18.6.
- **NOT a Hann crossfade.** That's architecture D-29 / Story 18.7.
- **NOT a Lightning Tier introduction.** That's Epic 19 (Phases L1–L3) per architecture's Cluster F.

### Source tree components to touch

**Read-only (analysis/reference):**
- `_bmad-output/planning-artifacts/architecture-streaming-acceleration-and-lightning-tier.md` — the canonical architecture for this story; Cluster E (D-21 through D-29 + P-10/P-11/P-12) is binding
- `_bmad-output/planning-artifacts/architecture-optimization-pass.md:803` (NFR3 cell — Place 1 of the two-place amendment); `:863-:899` (Story 17.1 / 18.3 follow-up note pattern — Place 2 mirror)
- `_bmad-output/planning-artifacts/research/technical-tts-streaming-fast-clone-research-2026-05-10.md` — the upstream-API discovery and audit context
- `_bmad-output/implementation-artifacts/18-3-bf16-precision-on-talker-decoder.md` — Story 18.3's full file (deferred audition framing; Task 7 measurement methodology; OQ #3 option (b) routing — the input to this story's joint-audition framing)
- `src/myvoice/services/qwen_tts_service.py:1145-:1693` (Story 17.2 voice_clone_prompt cache surface — the pin-bump must invalidate cleanly; the constant bump at `:1150` is the load-bearing change); `:3325-:3402` (the `_build_true_stream_decode_fn` Story 16.8 region — the dispatch chain stays unchanged); `:3404-:3571` (the talker forward-hook — the path `enable_streaming_optimizations()` will compile via the upstream API); `:582-:588` (the `ModelRegistry(...)` construction site Story 18.3 widened with `app_settings`)
- `src/myvoice/services/model_registry.py:60-:201` (the `__init__` Story 18.3 widened; Task 4 extends `_load_model_sync` and the INFO log line); `:438-:520` (the `_load_model_sync` body — the `engage_compile_optimizations` call lands here)
- `src/myvoice/services/tts_streaming/torch_runtime.py` (Story 18.2 + 18.3's module — Task 3 extends with `engage_compile_optimizations`)
- `src/myvoice/services/tts_streaming/__init__.py` (Story 18.2 + 18.3's exports — Task 3 widens with `engage_compile_optimizations` + the `compile_cache` re-export)
- `src/myvoice/services/tts_streaming/codec_token_streamer.py:46-:48` (`DEFAULT_CHUNK_SIZE = 25`, `DEFAULT_LOOKAHEAD = 5` — the literals that drive the D-25 invariant)
- `src/myvoice/services/tts_streaming/streaming_decoder.py` (Story 16.4 module — read-only; the dispatch consumer; not modified by Story 18.4)
- `src/myvoice/models/app_settings.py:103-:108` (`streaming_mode_override` precedent), `:108-` (post-Story-18.3 `tts_precision` declaration — Task 5 mirrors), `:375-:389` (validation block)
- `src/myvoice/app.py:445-:524` (Story 17.2 / 18.3 wire-up region — Task 6.7 extends with `warmup_compile_async()` task creation)
- `requirements.txt:23` + `build_tools/requirements-production.txt:61` (the qwen-tts pin — Task 1 bumps both)
- `tests/test_qwen_tts_internals.py:48-:60` (`expected_methods` list — Task 1.6 extends with `enable_streaming_optimizations`)

**New (source tree):**
- `src/myvoice/services/tts_streaming/compile_cache.py` (Task 2 — the seven-dim cache module; mirrors Story 18.3's `torch_runtime` module structure conceptually; lives in the same package)

**Edit (source tree):**
- `requirements.txt` (Task 1.3 — pin-bump)
- `build_tools/requirements-production.txt` (Task 1.4 — pin-bump)
- `tests/test_qwen_tts_internals.py` (Task 1.6 — `expected_methods` widened)
- `src/myvoice/services/qwen_tts_service.py` (Task 1.5 `_QWEN_TTS_PIN_HASH` constant bump; Task 6.1-6.6 `warmup_compile_async`)
- `src/myvoice/services/tts_streaming/torch_runtime.py` (Task 3 — `engage_compile_optimizations` function)
- `src/myvoice/services/tts_streaming/__init__.py` (Task 3.10 — exports widened)
- `src/myvoice/services/model_registry.py` (Task 4 — wire-up + INFO log extension)
- `src/myvoice/models/app_settings.py` (Task 5 — `tts_compile` field + validation + round-trip + reset list)
- `src/myvoice/app.py` (Task 6.7 — warmup task creation)

**New (tests):**
- `tests/unit/services/tts_streaming/test_compile_cache.py` (Task 2.9)
- `tests/unit/models/test_app_settings_tts_compile.py` (Task 5.5)
- `tests/unit/services/test_qwen_tts_service_compile_warmup.py` (Task 6.8)

**Edit (tests):**
- `tests/unit/services/tts_streaming/test_torch_runtime.py` (Task 3.11 — 8 new `engage_compile_optimizations` rows)
- `tests/unit/services/tts_streaming/test_codec_token_streamer.py` (Task 3.12 — `__all__` ordering append)
- `tests/unit/services/tts_streaming/test_streaming_decoder.py` (Task 3.12 — `__all__` ordering append)
- `tests/unit/services/test_model_registry.py` (Task 4.4 — 1 new compile-log test row)

**New (evidence + measurement + audition + harness artifacts):**
- `_bmad-output/implementation-artifacts/18-4-torch-compile-decoder-persistent-cache-evidence.md` (multi-section evidence file). Force-add per `memory/git_repo_state.md`
- `_bmad-output/implementation-artifacts/18-4-rtx5090-bf16-compile.csv` (Task 8.1; N=10). Force-add
- `_bmad-output/implementation-artifacts/18-4-rtx5090-bf16-eager.csv` (Task 8.1; N=10). Force-add
- `_bmad-output/implementation-artifacts/18-4-rtx5090-fp32-eager.csv` (Task 8.1; N=10). Force-add
- `_bmad-output/implementation-artifacts/18-4-perceptual-fixtures/` (Task 9.2; 10 paired WAVs + truth-table + LISTENING-INSTRUCTIONS copy). Force-add
- `_bmad-output/implementation-artifacts/18-4-l1-audition-helper.py` (Task 9.1; adapted from Story 18.3's helper). Force-add
- `_bmad-output/implementation-artifacts/18-4-bf16-compile-pinbump-audition.csv` (Task 9.4; 30 rows). Force-add
- `_bmad-output/implementation-artifacts/05_Story_18.4_NFR1_BF16_COMPILE.bat` + `06_Story_18.4_NFR1_BF16_EAGER.bat` + `07_Story_18.4_NFR1_FP32_EAGER.bat` (Task 8.1 harnesses)
- `_bmad-output/implementation-artifacts/18-4-set-precision-and-compile.py` (Task 8.2 settings.json mutator)
- `_bmad-output/implementation-artifacts/18-4-aggregate-nfr1.py` (Task 8.3 NFR1 3-way aggregator)

**Edit (evidence layer):**
- `_bmad-output/planning-artifacts/architecture-optimization-pass.md` (Task 10 two-place edit)
- `_bmad-output/planning-artifacts/architecture-streaming-acceleration-and-lightning-tier.md` (Task 10.4 D-22 Branch B EXECUTED marker)

**Edit (memory):**
- `memory/epic18_producer_bottleneck_finding.md` (Task 12.1 — close or update)
- `memory/build_tools_phase_perp_state.md` (Task 12.2 — re-validate or new follow-up)

**OUT OF SCOPE for Story 18.4 (separate stories per architecture D-38):**
- `src/myvoice/services/model_registry.py` FA2 verification probe (Story 18.5; D-26)
- `src/myvoice/services/tts_streaming/codec_token_streamer.py` two-phase emission scheduler (Story 18.6; D-28)
- `src/myvoice/services/tts_streaming/streaming_decoder.py` Hann crossfade post-decode (Story 18.7; D-29)
- `src/myvoice/services/tts_engines/` namespace and Lightning Tier work (Stories 19.1, 19.2, 19.3; Cluster F)

### Testing standards summary

- **Unit tests:** mirror Story 18.2 / 18.3's `test_torch_runtime.py` patterns. Monkeypatch `torch.cuda.is_available` and `torch.cuda.get_device_capability` at the attribute-access level. PyAudio / Qt / qwen-tts NOT involved in the unit-test surface (mock the model's `enable_streaming_optimizations` and the priming generation surface).
- **No real GPU required for unit tests.** All hardware-truth-table branches exercised via monkeypatch. Real-GPU validation lives in Tasks 7 + 8 + 9 (bundled smoke, NFR1 measurement, NFR3 audition).
- **Conftest discipline:** `tests/conftest.py` enforces torch-before-PyQt6 DLL ordering per `memory/torch_pyqt6_dll_ordering.md`. New test additions do NOT need a special preamble — they are pure-Python torch-attribute monkeypatching, no DLL-ordering surface area. If running under coverage, follow the inline torch-first preamble per `memory/torch_before_coverage_dll_ordering.md`.
- **AppSettings test discipline:** the `tts_compile` field's validation tests must mirror `tts_precision`'s test row structure exactly (the bug class is "validation drift between two near-identical fields"; `memory/code_review_regression_test_exact_class.md`).
- **Compile-cache test discipline:** the 7-dim key uniqueness test must use parametrization to vary EACH dimension independently (P-10 enforcement); a test that only exercises 3 of 7 dimensions and assumes the others are "obvious" would be the wrong fix class.
- **Decode-window-invariant test discipline:** the D-25 assertion test must exercise the HARD-FAIL path (raise `AssertionError`), not just the warning-log path. Per architecture P-11 — "loud at startup" is the contract.
- **Capability-probe test discipline:** the P-12 probe test must exercise BOTH branches: API succeeds + probe confirms engaged AND API succeeds + probe says "didn't actually engage" (the silent-no-op anti-pattern). Mock both surfaces.
- **Audition discipline:** the fixture regeneration MUST produce a NEW directory `18-4-perceptual-fixtures/` — do NOT overwrite or rename `16-7-perceptual-fixtures/` or `18-3-perceptual-fixtures/` (those preserve Story 17.1 and Story 18.3's audited verdicts' evidentiary surface).
- **Pin-bump regression discipline:** the Task 11.5 voice_clone_prompt cache test must exercise the pin-mismatch invalidation path EXPLICITLY — construct a `.pt.meta.json` with the OLD pin hash and assert `is_valid` returns False against the NEW constant. Per `memory/code_review_regression_test_exact_class.md` — exact-bug-class regression test.
- **NFR1 measurement-mode discipline:** the bf16-compile branch's first run is cold-compile (~10–30s additional latency); the aggregator script DISCARDS run #1 from the median and reports it separately. The warm-cache median is the canonical speedup number. Document this methodology nuance verbatim at evidence file §"NFR1 first-chunk-latency measurement (3-way A/B/C)".

### Project Structure Notes

**Alignment with unified project structure:**
- New `compile_cache.py` lives in `src/myvoice/services/tts_streaming/` — sibling of `streaming_mode.py` (Story 16.2), `codec_token_streamer.py` (Story 16.3), `streaming_decoder.py` (Story 16.4), `torch_runtime.py` (Stories 18.2 + 18.3). Same package; same import discipline; same test directory (`tests/unit/services/tts_streaming/`). Per architecture's "tts_streaming module boundary rule" at line 1157-1166 of `architecture-streaming-acceleration-and-lightning-tier.md` — "compile_cache.py may import: hashlib, pathlib, torch (cuda capability probe), stdlib; may NOT import services.*, tts_engines.*, ui.* (cache is a primitive; everything imports it; it imports nothing)".
- New `tts_compile` field lives in the existing `src/myvoice/models/app_settings.py` — alphabetically positioned alongside `tts_precision`.
- New `engage_compile_optimizations` lives in the existing `torch_runtime.py` — extending Story 18.2 + 18.3's module (NOT a parallel file). Same lazy-torch-import + side-effect-discipline + telemetry-tag-schema patterns.
- New `warmup_compile_async` lives in the existing `qwen_tts_service.py` near `hydrate_voice_clone_prompt_cache` (Story 17.2) — same fire-and-forget orchestrator-task pattern.
- New `app_settings` parameter on `ModelRegistry._load_model_sync` — backwards-compatible (default None preserves the legacy call surface, mirrors Story 18.3's `__init__` widening pattern).
- All new tests extend existing test directories — no new test directory.
- Evidence file at `_bmad-output/implementation-artifacts/18-4-torch-compile-decoder-persistent-cache-evidence.md` matches the per-story evidence-file pattern (Story 16.7 onward).

**Detected variances:**
- The qwen-tts pin-bump from `QwenLM/Qwen3-TTS@1ab0dd75` (the upstream main lineage) to `dffdeeq/Qwen3-TTS-streaming@<sha>` (a community fork) is a deliberate architectural variance from the V2 baseline's pin-discipline assumption that pins are upstream. Justified per: (a) architecture D-22 Branch B explicitly designs this branch; (b) the fork's `pyproject.toml` declares the same package name (`qwen-tts 0.0.4`) so it's a drop-in replacement at the import level; (c) the fork's diff is +50/-6 lines additive (no removed symbols); (d) the joint NFR3 audition closes the perceptual-equivalence question; (e) architecture D-33 outlines a quarterly check on whether Resemble has shipped an equivalent upstream API for `chatterbox-streaming` — applying the same discipline to qwen-tts: a quarterly check on whether `QwenLM/Qwen3-TTS` upstream picks up the patch (e.g., via PR from the fork author) → if yes, swap the pin back to upstream + drop the community pin discipline. Documented at evidence file §"Pin-bump rationale" + memory entry to add post-shipment.
- The new `compile_cache.py` introduces a per-key directory structure under `%LOCALAPPDATA%/MyVoice/torch_compile_cache/` — first time MyVoice writes a "compute-cache" surface (vs Story 17.2's "data-cache" surface). The directory is gitignored (under `_bmad-output/` lineage); architecture D-24 names "no size cap in v1; future enhancement if disk usage becomes a concern" (line 355 of architecture doc).
- **No D-decision change.** Story 18.4 does NOT require a D-decision amendment per architecture's "Critical decisions made in Step 4" framing — D-21 through D-29 are already bound; Story 18.4 EXECUTES them. The architecture amendment at Task 10.4 marks D-22 Branch B as EXECUTED, not modified.

### Previous Story Intelligence

**From Story 18.3 closure (commit `e6de30e` + the empirical-null-on-bf16 outcome + the deferred-audition framing):**

- Story 18.3 closed with bf16 engaged on Ampere+ (logged at `myvoice.log` with `precision_source='app_settings_auto_ampere'`) but the empirical first-chunk-latency speedup was **-3.77%** (slightly slower) on the producer-bottleneck workload, with steady-state ratio bf16=1.62 vs fp32+TF32=1.40. **Translation for Story 18.4:** the bf16 baseline this story composes against is engaged-but-empirically-null on RTX 5090; the deferred audition (Story 18.3 Task 8) was the input gate for whether bf16 stays default or rolls back. Story 18.4's joint-audition framing **closes that deferred audition** + the compile audition + the pin-bump audition in a single cycle. The fp32-eager branch in Task 8's 3-way A/B/C measurement is the strictest baseline available post-pin-bump; the bf16-compile branch is the production target.
- Story 18.3's `AppSettings.tts_precision` field + `resolve_tts_precision` resolver + `tts_precision_resolved` telemetry + four `precision_source` labels are the canonical pattern Story 18.4 mirrors for `tts_compile` + `engage_compile_optimizations` + `tts_compile_engaged` telemetry + the `reason` enum (8 values for compile vs 4 for precision — same conceptual schema).
- Story 18.3's `MYVOICE_AUTO_QUIT_ON_CLOSE=1` measurement-mode env-var pattern (the X-button bypass for the N=10 NFR1 loop) is **directly reused** by Story 18.4's `05/06/07_*.bat` harnesses. Reuse the existing `MainWindow.closeEvent` env-var gate — no need to widen.
- Story 18.3's M2 + M6 drain-math fixes (the `_DRAIN_SAFETY_BUFFER_S = 0.5` + `_stream_last_*` per-chunk trackers + the M6 producer-bottleneck-workload regression test) **stay applicable** to Story 18.4's measurement runs — the cut-off-at-end finalization race is fixed at the AudioCoordinator level and does not regress with compile engaged.
- Story 18.3's H1 fix (the production wire-up regression at `app.py:445` where the `QwenTTSService(...)` constructor dropped `app_settings=`) **stays applicable** — Story 18.4's `app.py` edits at `:445-:524` (Task 6.7) must NOT regress that fix. The static-scan test at `tests/unit/test_app_qwen_tts_construction.py` (3 rows) is the canonical regression coverage; if Story 18.4 adds a new keyword argument to `QwenTTSService(...)` (e.g., the warmup-callback wire-up), extend the static-scan test accordingly.
- Story 18.3's code-review pass surfaced 5 MEDIUM (M1-M6, no HIGH after H1 was auto-fixed). **Story 18.4 should anticipate similar review-finding categories** — the Task 13 code-review pass enumerates 9 anticipated categories per `memory/code_review_regression_test_exact_class.md`'s exact-bug-class discipline.

**From Story 18.2 closure (commit `7300c6f` + the empirical-null-but-shipped outcome):**

- Story 18.2 shipped TF32 + cuDNN benchmark engaged on RTX 5090 (logged at `myvoice.log:3` with `device_capability=12.0`) and produced the producer-bottleneck ratio drop from 3.23× (Story 18.1 baseline) to 1.40× (post-Story-18.2). **Translation for Story 18.4:** the fp32+TF32-engaged baseline (Story 18.4 measurement branch C) is the strictest available; the bf16+TF32 baseline (branch B) is Story 18.3's residue; the bf16+compile+TF32 branch (A) is the production target. The 3-way A/B/C measurement composes ALL THREE Story-18.x optimizations.
- Story 18.2's `enable_tf32_and_cudnn_benchmark` lazy-torch-import discipline + side-effect-free pure-decision pattern + telemetry tag schema (`device_capability` `"none"` sentinel for CPU per OQ #2) are the canonical patterns Story 18.4 mirrors.
- Story 18.2's deferred bundled smoke (Task 6) was **composed** with Story 18.3's Task 10 — Story 18.4's Task 7 continues the composition: the FIRST build-state commit cycle that lands all of {Story 18.2 + 18.3 + 18.4 source-tree edits} is the bundled-smoke verification surface. Coordinate with Commander on the build-state commit timing.

**From Story 18.1 closure (commit `956c039` + the producer-bottleneck verdict):**

- Story 18.1's evidence file §4.4 pinned the producer bottleneck at 3.23× steady-state ratio with the talker model decode rate at 31% real-time. Story 18.4 (compile) is named as the **second** of two fix-class members (alongside 18.3 bf16, which empirically didn't pay). **Translation for Story 18.4:** the architecture's OFR-E target is "ratio < 1.0× sustained on RTX 5090"; Story 18.4 is the last shot in the epic before retro. If Task 8's measurement closes the gap, the memory entry at `memory/epic18_producer_bottleneck_finding.md` is MARKED CLOSED. If not, the memory entry stays open and Open Question #1 routes to Commander.
- Story 18.1's `M1` (`AudioChunk.session_id`) and the consumer-side metric session_id threading are NOT touched by Story 18.4. The new metric `tts_compile_engaged` is a startup-once event (mirrors Story 18.2 + 18.3's startup-once metric pattern), not a per-chunk metric.

**From Story 17.1 + 17.2 + 17.3 (audition + cache-key + indicator-pattern discipline):**

- Story 17.1's two-place architecture amendment pattern at `architecture-optimization-pass.md:863-:899` is the canonical template for Task 10. The NFR3 cell parenthetical + the new follow-up note structure mirror it verbatim. Story 18.3's amendment kept Story 17.1's parenthetical and APPENDED 18.3's; Story 18.4 does the same — APPENDS to Story 18.3's parenthetical (cumulative pattern preserves the document's reading flow).
- Story 17.1's `17-1-l1-audition-helper.py` is the canonical helper-script template (Story 18.3 adapted as `18-3-`; Story 18.4 adapts as `18-4-`). The fixture regeneration discipline (NEW directory; do NOT overwrite the old fixture) is mandated.
- Story 17.1's M1 reproducibility note bounds the verdict's reproducibility — future maintainers re-validating need either the original fixture or a fresh full audit. Story 18.4's fixture is the SAME pattern: gitignored under `_bmad-output/`; held only on Commander's filesystem; force-added via `git add -f`.
- Story 17.2's H1 / H2 cache-invalidation discipline is **directly applicable** to Story 18.4's `compile_cache.py` (per `memory/code_review_regression_test_exact_class.md`). The 7-dim cache key (D-24) explicitly invalidates on any of seven dimension changes; the `meta.json` sidecar pattern Story 17.2 introduced is mirrored. **Two H1/H2 regression tests must mirror Story 17.2's bug class:** (a) a wrong-key meta-file is detected as "not warm" (mirrors H1 — in-memory cache hit gated on disk meta validity); (b) a directory-prefix collision (key1[:16] == key2[:16] but full keys differ) is detected as "not warm" (mirrors H2 — sidecar mtime missing from cache key was H2's exact bug; the prefix-collision is the moral equivalent for `compile_cache`).
- Story 17.2's `_emit_preparing_voice` callback is **directly reused** by Story 18.4's warmup worker — the existing `set_preparing_voice_callback` orchestrator wire-up at `app.py:457-:459` is the canonical surface for the "Preparing TTS engine…" indicator. Mirrors Story 17.2's "Preparing voice for streaming…" pattern.
- Story 17.3 closed the progressive-playback contract; Story 18.4 inherits the canonical Sarira-F long-form utterance methodology (≥250 chars / ~22 s of speech) for the NFR1 measurement at Task 8.

**Code-review discipline from `memory/code_review_regression_test_exact_class.md`:**

- HIGH/MEDIUM-fix regression tests must mirror the **exact** bug class. For Story 18.4, the highest-risk regression classes are:
  - **Cache-key dimension drift** (P-10 violation) — multiple call sites constructing the 7-dim key would let dimensions silently diverge. Task 2.9's "key uniqueness" test (one parametrized row per dimension) is the canonical regression coverage. NOT a test that varies 3 dimensions and assumes the rest are "obvious."
  - **Decode-window invariant silent fallthrough** (P-11 violation) — a `log.warning(mismatch)` followed by silent continuation produces wrong audio. Task 3.11's D-25 test must exercise the HARD-FAIL `AssertionError` path, not the warning-log path.
  - **NFR7 fallback completeness** — the 8-branch test row coverage at AC #11 (the `reason` enum) is the canonical surface; missing one branch is the failure mode.
  - **Pin-bump constant drift** — bumping `requirements.txt` without bumping `_QWEN_TTS_PIN_HASH` lets Story 17.2 cached `.pt` files load with wrong-pin meta sidecars. Task 11.5's regression test is the canonical pin-mismatch coverage.
- Re-run code-review twice after non-trivial auto-fixes (the established pattern from Stories 16.7 / 16.8 / 17.1 / 17.2 / 17.3 / 18.1 / 18.2 / 18.3).

### Latest Tech Information

**`enable_streaming_optimizations()` upstream API:**
- The official API lives in the community fork `dffdeeq/Qwen3-TTS-streaming` at commit `3fdb4682` ("compile and fast codebook", 2026-02-03). Drop-in replacement for the canonical `qwen-tts 0.0.4` package — same name, same import paths, same `Qwen3TTSModel` class. _Source: 2026-05-10 research subagent verification + the fork's `pyproject.toml`._
- Real signature: `enable_streaming_optimizations(decode_window_frames=80, use_compile=True, use_cuda_graphs=True, compile_mode="reduce-overhead", use_fast_codebook=False, compile_codebook_predictor=True, compile_talker=True)` — broader than the architecture's three named kwargs (`decode_window_frames`, `use_compile`, `compile_mode`). Story 18.4 calls only the three named kwargs; the fork's defaults handle the rest. **Architecture D-21 binds `decode_window_frames=30`** (not the fork default 80) per the streamer's existing `chunk_size + lookahead = 30`. The fork accepts arbitrary `decode_window_frames` values per `dffdeeq/Qwen3-TTS-streaming@3fdb4682` source — verified at impl time per architecture's "verification gate (impl-time, not architecture-time)" framing at line 323.
- Reported gain: **2.15× per-frame** on the codebook predictor (12.94 ms → ~6 ms compiled). _Source: dffdeeq fork README + research-cited community-fork TTFA improvements 5–10× when paired with the existing sliding-window streamer._

**`torch.compile` + CUDA Graph capture (PyTorch 2.10):**
- `compile_mode="reduce-overhead"` automatically captures CUDA graphs for repeated forward passes. Combined with a fixed `decode_window_frames=N`, this enables full graph replay without recompilation — the foundation of the multi-× speedups in the streaming forks.
- `dynamic=True` (the original epic-stub design) is **mutually exclusive** with CUDA Graph replay. The architecture rejects this option (D-21 sub-option (c)).
- First-call compile cost: 10–30s wall time per the research synthesis. Mitigated by D-23's background warmup + persistent compile cache (TORCHINDUCTOR_CACHE_DIR env var).
- Cache cold-vs-warm distinction: the inductor cache directory contains compiled-graph artifacts keyed on the exact graph shape + dtype + capability + torch version. The 7-dim `compile_cache.compute_key()` (architecture D-24) is the high-level cache key MyVoice maintains; the underlying inductor cache (managed by PyTorch) is mapped 1:1 to MyVoice's per-key directory via `set_torchinductor_cache_dir(key)`.
- VRAM impact: marginal for inference (no gradient-capture overhead). The compile cache itself lives on disk, not VRAM. Per-chunk forward passes hit the compiled graph in VRAM at runtime; the cold compile populates the disk cache so subsequent process launches reload from disk in ~1s rather than re-running the 10–30s compile.

**`AppSettings.tts_compile` semantics:**
- `"auto"` (default) defers to the hardware probe + compile-failure fallback: engages on Ampere+ CUDA when `enable_streaming_optimizations` is callable; falls through to eager on any failure. Mirrors `tts_precision = "auto"`'s discipline.
- `"on"` is user-explicit opt-in — engages on Ampere+ CUDA. Same hardware gate; CPU users still get eager mode (the hardware-emulation slowdown is a non-engagement reason).
- `"off"` is the NFR7 fallback path: forces eager-mode even on Ampere+. The user has observed a compile-related defect post-release (perceptual or correctness) and wants the lossless path. Mirrors `tts_precision = "fp32"`'s discipline.
- `"auto"` is the default because the joint audition (Task 9) is the certification surface — a passing audition means compile is safe-by-default on Ampere+.

**Persistent compile cache discipline (architecture D-24):**
- Cache key: 7 dimensions per AC #3. SHA-256 hex digest joined by `"|"`. Constructed in EXACTLY one place (`compile_cache.compute_key()`; P-10).
- Cache root: `%LOCALAPPDATA%/MyVoice/torch_compile_cache/` on Windows (mirrors V2 baseline's `%LOCALAPPDATA%/MyVoice/` storage discipline).
- Cache invalidation: any of seven dimension changes (qwen_tts_pin_hash bump, model_id change, precision_str toggle, torch_version upgrade, decode_window_frames re-tune, hardware tier change, compile_mode change) → cache miss → re-compile on next priming.
- No size cap in v1 (architecture D-24 explicit — "future enhancement if disk usage becomes a concern"). Each compile-cache entry is ~50-200 MB per the inductor cache pattern; expected steady-state size is single-digit GB across the lifetime of a torch upgrade cycle.
- Per-key directory pattern (`cache_dir_for_key(key)` returns `cache_root / key[:16]`) defends against directory collisions; full-key match in `meta.json` is the canonical "warm" signal.

**Pin-bump rationale (architecture D-22 Branch B):**
- The pin-bump from `QwenLM/Qwen3-TTS@1ab0dd75` to `dffdeeq/Qwen3-TTS-streaming@3fdb4682` is a **deliberate** architectural variance from the V2 baseline's "pins are upstream" assumption. Justified by:
  - The fork's `pyproject.toml` declares the same package name (`qwen-tts 0.0.4`) — drop-in replacement at the import level.
  - The fork's introducing diff is +50/-6 lines additive (no removed symbols MyVoice depends on) — verified at Task 1.2 by reading the diff at `https://github.com/dffdeeq/Qwen3-TTS-streaming/commit/3fdb4682`.
  - The joint NFR3 audition (Task 9) closes the perceptual-equivalence question — the audition certifies the new pin's output sounds equivalent to the old pin's output (under bf16+compile production conditions, with the strict-eager-fp32 baseline as the "A" rendition).
  - Story 16.8's `_build_true_stream_decode_fn` dispatch chain is preserved verbatim — the fork's added `stream_generate_pcm()` / `stream_generate_voice_clone()` APIs are NOT adopted; Story 18.4 stays on the existing forward-hook + CodecTokenStreamer pipeline.
- **Quarterly check** (analogous to architecture D-33's `chatterbox-streaming` discipline): check whether `QwenLM/Qwen3-TTS` upstream picks up the patch (e.g., via PR from the fork author). If yes, swap the pin back to upstream + drop the community-pin discipline. Memory entry to add post-shipment.

**Joint audition methodology:**
- Single ≥3-listener cycle certifies bf16 + compile + pin-bump simultaneously (closes Story 18.3's deferred audition + D-22 Branch B re-audition + Story 18.4's primary perceptual gate).
- Fixture regeneration produces `18-4-perceptual-fixtures/` (NEW directory; preserves `16-7-` and `18-3-` fixtures unchanged).
- "A" rendition: new pin + fp32 + eager (strictest available baseline post-pin-bump). "B" rendition: new pin + bf16 + compile (production target).
- Total trials = 30 (10 utterances × 3 listeners × A and B labeled blind per Story 17.1's helper-driven protocol).
- Verdict gate: PASS iff zero `audible_seam` flags on bf16+compile+new-pin pair across all 30 trials. Verbatim Story 17.1.
- FAIL routing requires a follow-up A/B test that varies one dimension at a time on the failed utterance to identify which dimension correlates with the defect (bf16 vs compile vs pin-bump). Open Question #2.

### Project Context Reference

- Project context: `docs/` (existing project-context.md not found; CLAUDE.md absent).
- Working directory invariants per `memory/git_repo_state.md`: V2 is canonical git repo since 2026-05-05; remote = github.com/WreckedMech117/MyVoice; `_bmad-output/` is gitignored (evidence file + CSVs + fixture + audition CSV all need `git add -f`).
- Production state per `memory/production_release_state.md`: ships publicly via myvoicetts.com as a Windows .exe with bundled portable python310. Installer size unchanged by Story 18.4 (the persistent compile cache lives at `%LOCALAPPDATA%`, NOT bundled in the installer; cold-compile on first run after install is the architectural pattern, mirroring the Whisper model download pattern). Build-counter increments at `build_tools/installer.iss` + `build_tools/version.py` are Commander-handled in a separate build-state commit.
- Hardware target per `memory/hardware_setup.md`: RTX 5090 Blackwell (compute 12.0 per Story 18.2's measurement) dev host; ship-target covers RTX 30xx (compute 8.6) / RTX 40xx (compute 8.9). All three satisfy the new probe's `>= 8` Ampere+ gate. CPU and pre-Ampere users skip compile entirely (D-9 / NFR12 inheritance).
- Phase context per `memory/build_tools_phase_perp_state.md`: Phase ⊥-Polish-2 is the successor to Phase ⊥-Polish (Story 17.3 closed the progressive-playback contract); Story 18.4 is the **fourth and final** story of Phase ⊥-Polish-2 (after Story 18.1 instrumentation + Story 18.2 TF32+cuDNN + Story 18.3 bf16). Story 18.4 closes Epic 18 (or routes to OQ #1 if compile doesn't pay).
- Producer-bottleneck context per `memory/epic18_producer_bottleneck_finding.md`: Story 18.1's 3.23× ratio at the producer side names 18.3 + 18.4 as the fix class. Story 18.3 closed nothing (-3.77%); Story 18.4 is the last shot. Architecture OFR-E target is "ratio < 1.0× sustained on RTX 5090".
- Architecture context: `architecture-streaming-acceleration-and-lightning-tier.md` (sealed 2026-05-10) is the canonical source for D-21 through D-29 + P-10/P-11/P-12 that Story 18.4 implements. Read alongside `architecture-optimization-pass.md` (sealed 2026-04-27) and `architecture.md` (V2 baseline, sealed 2026-01-31).

### References

- **Architecture (sealed 2026-05-10)** — `_bmad-output/planning-artifacts/architecture-streaming-acceleration-and-lightning-tier.md` Cluster E (D-21 through D-29: lines 318–401); P-10 (single-helper cache key, lines 594–626); P-11 (invariant assertions, lines 628–672); P-12 (capability verification, lines 674–713); migration map (D-38, lines 540–556); module boundaries (lines 1118–1186); test additions table (line 1273+). The canonical source.
- **Parent architecture** — `_bmad-output/planning-artifacts/architecture-optimization-pass.md` lines 803 (NFR3 cell — Place 1 of two-place amendment), 863–899 (Story 17.1 + 18.3 follow-up note pattern — Place 2 mirror), 257 (D-9 hardware-aware streaming default), 75 + 808 (NFR12 CPU-only support), 286+ (D-19 telemetry), 802 + 838–850 (NFR1 revised contract).
- **V2 baseline architecture** — `_bmad-output/planning-artifacts/architecture.md` (sealed 2026-01-31). Storage path discipline (`%LOCALAPPDATA%/MyVoice/`); single-model lazy-load; PyQt6 + asyncio threading model.
- **Research note** — `_bmad-output/planning-artifacts/research/technical-tts-streaming-fast-clone-research-2026-05-10.md` lines 580–597 (the upstream API discovery + reported gain), 562 (community-fork audit's "torch.compile / CUDA Graph: NO" finding + the "Story 18.4 explicitly deferred" framing), 596 (the "Story 18.4 framing: half-day PR" caveat — re-evaluated in this story given the pin-bump + audition scope expansion).
- **Epic 18 stub** — `_bmad-output/planning-artifacts/epics-optimization-pass.md` lines 228–250 (Epic 18 framing); 1390–1411 (Story 18.4 stub — supersedes the `dynamic=True` design with the architecture's D-21 fixed-window choice); 240 (recommended sequencing); 245–246 (risk profile); 248 (build-pipeline implications — explicitly out of scope for 18.1-18.3 but Story 18.4 IS the pin-bump branch per D-22 Branch B); 1402–1411 (Story 18.4 stub's anticipated acceptance gate, dependencies, files-likely-touched).
- **Story 18.3 evidence** — `_bmad-output/implementation-artifacts/18-3-bf16-precision-on-talker-decoder.md` (full file; Tasks 1, 5, 7, 8, 9 framing patterns; deferred-audition rationale; Open Question #3 routing); `_bmad-output/implementation-artifacts/18-3-bf16-precision-on-talker-decoder-evidence.md` (the canonical evidence-file structure Story 18.4 mirrors — Pre-implementation audit, NFR1 first-chunk-latency measurement, Bundled smoke, etc.).
- **Story 18.2 evidence** — `_bmad-output/implementation-artifacts/18-2-tf32-cudnn-benchmark-enable-evidence.md`. The TF32-engaged baseline this story composes against (the fp32-eager branch C is fp32+TF32+cuDNN-engaged, not strict-fp32-only).
- **Story 18.1 evidence** — `_bmad-output/implementation-artifacts/18-1-underrun-gap-mitigation-evidence.md` §4.4. The producer-bottleneck verdict naming Story 18.4 as the second fix-class member.
- **Story 17.1 audition + architecture-amendment** — `architecture-optimization-pass.md:863-:899` (the two-place edit pattern Task 10 mirrors); `_bmad-output/implementation-artifacts/17-1-perceptual-ab-results.csv` (the audition-CSV format Task 9.4 mirrors); `_bmad-output/implementation-artifacts/17-1-l1-audition-helper.py` (the helper-script template Task 9.1 adapts via Story 18.3's adaptation).
- **Story 17.1 fixture** — `_bmad-output/implementation-artifacts/16-7-perceptual-fixtures/` (the canonical 10-pair fixture; Story 18.4 regenerates as `18-4-perceptual-fixtures/` against bf16+compile+new-pin output, preserves the 16-7 directory and 18-3-perceptual-fixtures unchanged per Story 17.1 / 18.3 reproducibility notes).
- **Story 17.2 voice_clone_prompt cache** — `src/myvoice/services/qwen_tts_service.py:1145-:1693` (the `_voice_clone_prompt_persist_paths` / `_voice_clone_prompt_meta_is_valid` / `_QWEN_TTS_PIN_HASH` constant — the load-bearing pattern Story 18.4's `compile_cache.py` mirrors, AND the surface the pin-bump invalidates). `_bmad-output/implementation-artifacts/17-2-cloned-voice-truestream-prompt-precompute.md` lines 122 (`_QWEN_TTS_PIN_HASH = "1ab0dd75"` constant); 156–159 (H1 + H2 review-pass discipline).
- **Story 16.2 streaming-mode hardware probe** — `src/myvoice/services/tts_streaming/streaming_mode.py:54-:56`; the structural precedent for the D-9 hardware gate `engage_compile_optimizations` mirrors.
- **Story 18.2 / 18.3 torch_runtime module** — `src/myvoice/services/tts_streaming/torch_runtime.py`; `tests/unit/services/tts_streaming/test_torch_runtime.py`. The module Story 18.4 extends with `engage_compile_optimizations` (NOT a parallel file; same module per the architecture's module-boundary table).
- **Architecture D-9 hardware-aware default** — `architecture-optimization-pass.md:257`. The `torch.cuda.is_available()` probe + Ampere+ guard discipline. Story 18.4's `tts_compile = "auto"` branch enforces this for the compile dimension, closing the latent CPU-compile-attempted violation.
- **Architecture NFR3 perceptual-gate** — `architecture-optimization-pass.md:803`. Task 10 amends this cell with the Story 18.4 joint-audition outcome (cumulative pattern preserves the Story 17.1 + 18.3 parentheticals).
- **Architecture NFR7 graceful degradation** — `architecture-optimization-pass.md:806`. Story 18.4's `tts_compile = "off"` override + the 8-branch eager-fallback enum is the compile-tier extension of NFR7 (analogous to Story 18.3's `tts_precision = "fp32"` extension).
- **Architecture NFR12 CPU-only support** — `architecture-optimization-pass.md:75 + :808`. CPU-only hosts stay identifiably unchanged; the `tts_compile = "auto"` CPU branch returns eager (closing the latent V2 default that would have attempted compile unconditionally).
- **Architecture NFR1 revised contract** — `architecture-optimization-pass.md` §"NFR1 (revised 2026-05-08, Story 16.9)" at lines 838–850. Per-class first-chunk targets; Story 18.4's Task 8 measures the bf16+compile vs bf16-eager vs fp32-eager deltas against this baseline.
- **Architecture D-19 telemetry** — `architecture-optimization-pass.md` §"D-19 Telemetry" (begins at line 286) and the `metrics.record(name, value, **tags)` helper specified at line 476. Implementation lives at `src/myvoice/observability/metrics.py:77`. Story 18.4's new `tts_compile_engaged` + `tts_compile_warmup_priming` metrics extend this established pattern.
- **AppSettings `tts_precision` precedent (Story 18.3)** — `src/myvoice/models/app_settings.py:103-:108` (declaration), `:375-:389` (validation). The mirror surface Task 5 uses verbatim for the new `tts_compile` field.
- **AppSettings `streaming_mode_override` precedent (Story 16.6)** — same file. The deeper precedent Story 18.3's `tts_precision` itself mirrors.
- **ModelRegistry production code path** — `src/myvoice/services/model_registry.py:60-:201` (the `__init__` Story 18.3 widened); `:438-:520` (`_load_model_sync` body — the wire-up site for Story 18.4); `:198-:201` (the existing INFO log — Story 18.4 extends with `compile_engaged='{state}'`).
- **QwenTTSService construction site** — `src/myvoice/app.py:445-:524` (the Story 18.3 H1 region); `src/myvoice/services/qwen_tts_service.py:582-:588` (the `ModelRegistry(...)` call inside).
- **Streaming decoder consumer contract** — `src/myvoice/services/tts_streaming/streaming_decoder.py:82` (the `decode_fn: Callable[[list[Any]], np.ndarray]` type); `:1-:36` (the module docstring's Story 16.4 framing). Read-only for Story 18.4.
- **Streaming decoder supplier site** — `src/myvoice/services/qwen_tts_service.py:3325-:3402` (`_build_true_stream_decode_fn`, Story 16.8 origin). The dispatch chain stays unchanged. Read-only for Story 18.4.
- **Memory: hardware_setup.md** — RTX 5090 Blackwell (Commander measurement: compute 12.0 GeForce variant) as dev host; ship-target covers RTX 30xx / RTX 40xx. All three satisfy the `>= 8` Ampere+ gate.
- **Memory: torch_pyqt6_dll_ordering.md** — the Windows DLL-init invariant the Task 4 wire-up preserves (the new code is pure-Python torch-attribute access; no DLL-ordering surface).
- **Memory: build_tools_phase_perp_state.md** — Phase ⊥-Polish-2; Story 18.4 is the fourth and final story of the phase. Build counter bump from 12 → 13 is Commander-handled in a separate build-state commit.
- **Memory: code_review_regression_test_exact_class.md** — exact-bug-class regression-test discipline. The Task 13 code-review pass enumerates 9 anticipated review-finding categories per this discipline.
- **Memory: production_release_state.md** — production-bundle context informing Task 7 (bundled-smoke). Installer size unchanged (compile cache at `%LOCALAPPDATA%`, not bundled).
- **Memory: epic18_producer_bottleneck_finding.md** — the 3.23× ratio context Story 18.4 closes (or routes to OQ #1 if compile doesn't pay either). Task 12.1 updates this memory entry conditionally on Task 8 outcome.
- **Memory: git_repo_state.md** — V2 git-repo state; `_bmad-output/` gitignored (force-add discipline for evidence + CSVs + fixture + audition CSV).
- **Memory: main_window_close_confirm_dialog_in_tests.md** — the `MYVOICE_AUTO_QUIT_ON_CLOSE` env-var precedent Task 8 reuses for the N=10 measurement loops (Story 18.3 introduced; Story 18.4 inherits).

## Open Questions

(The dev agent saves discovered questions here for Commander-routing rather than absorbing them unilaterally. Per the `instructions.xml` "❓ SAVE QUESTIONS" critical rule.)

1. **NFR1 measurement falls below 20% speedup OR producer ratio (branch A) does not drop below 1.0× sustained (Task 8.6 routing).** The architecture's OFR-E acceptance gate is "ratio < 1.0× sustained on RTX 5090"; the epic stub's anticipated 1.5–3× warm-cache decode speedup is informational not pass/fail. Sub-20% specifically suggests the compile didn't apply to the talker's autoregressive loop (where Story 18.1's bottleneck lives) and a different compile target is needed. The fork's `enable_streaming_optimizations` API claims to compile both `compile_codebook_predictor` AND `compile_talker` (default True for both per the Task 1.2 diff inspection); if the measurement contradicts this claim, the fork's claim is unverified. Commander decides whether to (a) ship-as-engaged-anyway with the bf16-default-rolled-back since neither paid, (b) defer to a future investigation with a different compile target (e.g., a MyVoice-side `torch.compile(model.model.talker.forward)` wrap), (c) close the epic with `tts_compile = "off"` as default + retro entry capturing the producer-bottleneck-not-closed-this-pass verdict, (d) escalate to upstream `dffdeeq` fork investigation (the fork's claim of 2.15×/predictor may not generalize to autoregressive workloads).

2. **NFR3 joint audition flags `audible_seam` on any utterance (Task 9.6 routing).** Verdict FAIL → outcome (b) per Story 17.1's framing. The dev agent does NOT unilaterally close the story; route with the failed-utterance class AND the failed-dimension class annotated. Commander decides whether to (i) defer the bf16-default flip + ship compile-as-default + roll back the pin-bump (only one dimension fails), (ii) defer the bf16-default + compile-default flips and ship `tts_compile = "off"` + `tts_precision = "fp32"` as the new defaults (multiple dimensions fail), (iii) escalate to upstream `dffdeeq` fork investigation, (iv) accept the defect with a session-class fp32-eager fallback for the affected utterance class. Each option has different cost / benefit / risk profiles that exceed the dev agent's autonomous-decision scope.

3. **Pin-bump regression surfaces in the broader test sweep (Task 1.9 routing) OR voice_clone_prompt cache test (Task 11.5 routing).** A regression here means the fork is NOT actually a drop-in replacement and the pin-bump scope expands beyond Story 18.4's scope. Commander decides whether to (a) absorb the regression with a follow-up fix in this story (acceptable if the fix is mechanical and small), (b) split Story 18.4 into "verify + plan" + "execute + re-audition" tracks (architecture's named fallback per the "Story 18.4's first AC is the verification step; the story splits if Branch B fires" framing at architecture line 1381 — extended to "if a regression surfaces during Branch B execution, the story splits further"), (c) revert to the upstream pin and reimplement the API locally as a MyVoice-side `torch.compile` wrap of `model.model.speech_tokenizer.decode` (architectural variance from D-22 — would require a follow-up architecture amendment).

4. **P-12 capability probe reveals the fork's `enable_streaming_optimizations` is a no-op (Task 3.7 routing).** The fork's added 50 lines may be a stub that doesn't actually engage compile (the API call returns without raising but the model state is unchanged). The probe is the canonical signal. If the probe captures "API didn't engage despite no exception," route to Commander. Options: (a) revert the architectural decision (use upstream API) and reimplement locally (per OQ #3 option (c)), (b) bump to a different fork commit that actually implements the API (the architecture subagent identified `dffdeeq/Qwen3-TTS-streaming@aad92ef8` as HEAD; the introducing commit `3fdb4682` may or may not be the mature implementation), (c) accept eager-mode fallback as the steady-state and close Story 18.4 as "compile-API-engaged-but-no-op-on-this-fork" + retro entry capturing the architectural lesson.

## Dev Agent Record

### Agent Model Used

Opus 4.7 (1M context) — `claude-opus-4-7[1m]`.

### Debug Log References

- Pin-bump verification: `gh api repos/dffdeeq/Qwen3-TTS-streaming/commits/3fdb4682` → full SHA `3fdb468233d73fa537202b94a1cc7c4e7a6160b8`; diff is 3 files / +50/-6 lines additive.
- Pre-bump grep confirmation: `grep -rn "enable_streaming_optimizations|use_compile|compile_mode" python310/Lib/site-packages/qwen_tts/` returned ZERO matches at pin `1ab0dd75`.
- Post-bump API reachability: `python310/python.exe -c "from qwen_tts import Qwen3TTSModel; assert callable(getattr(Qwen3TTSModel, 'enable_streaming_optimizations', None))"` → OK.
- P-12 probe surface: chose `_torchdynamo_orig_callable` attribute on `model.model.talker.model.forward` (PyTorch 2.x canonical wrapped-function sentinel since 2.0). Defensive fallback: also accepts `__wrapped__` and `_orig_mod`.
- Subprocess key-stability test issue: bare `import myvoice.services.tts_streaming.compile_cache` in a subprocess triggers the full UI → qwen-tts → torch import chain and hits the Windows DLL ordering error. Fixed by direct-importing `compile_cache.py` via `importlib.util.spec_from_file_location` so the subprocess sidesteps `myvoice/__init__.py`.
- `_PlainObj` helper class needed in `test_torch_runtime.py`: MagicMock auto-creates any attribute on access, so `hasattr(mock.fwd, "_torchdynamo_orig_callable")` is always True. Replaced the talker dereference chain with plain attribute-controlled objects to make the probe-failed branch testable.
- Story 18.3 H1 protection: app.py static-scan tests at `tests/unit/test_app_qwen_tts_construction.py` still pass 3/3 after adding the `warmup_compile_async` fire-and-forget task creation — no regression of the `QwenTTSService(... app_settings=self._app_settings ...)` invariant.

### Completion Notes List

**Dev-agent-autonomous tasks (Tasks 1, 2, 3, 4, 5, 6, 8 harness, 9 helper, 11) — CLOSED.**

* **Task 1 — D-22 verification + pin-bump:** Empirically re-verified that `enable_streaming_optimizations` does NOT exist at the old pin (zero grep matches across `python310/Lib/site-packages/qwen_tts/`). D-22 Branch B fires. Pin bumped from `QwenLM/Qwen3-TTS@1ab0dd75353392f28a0d05d9ca960c9954b13c83` to `dffdeeq/Qwen3-TTS-streaming@3fdb468233d73fa537202b94a1cc7c4e7a6160b8` in `requirements.txt:23` + `build_tools/requirements-production.txt:61`. `_QWEN_TTS_PIN_HASH` constant bumped to `"3fdb4682"`. `tests/test_qwen_tts_internals.py` extended with the new `enable_streaming_optimizations` row in `expected_methods`. 9/9 internals tests pass; 211/211 focused regression-smoke tests pass.
* **Task 2 — `compile_cache.py` module:** Created with the seven-dim `compute_key` (SHA-256), Windows-aware `cache_root` (`%LOCALAPPDATA%/MyVoice/torch_compile_cache/`), per-key `cache_dir_for_key`, `is_warm` / `mark_warm` (with the H1+H2 cache-invalidation discipline mirroring Story 17.2), and the idempotent `set_torchinductor_cache_dir`. 16/16 unit tests pass (covering 7 AC #3 sub-bullets: stability across invocations + across processes; uniqueness parametrized one-per-dimension; Windows + non-Windows roots; round-trip; collision defense; unparseable-meta defense; idempotency).
* **Task 3 — `engage_compile_optimizations` in `torch_runtime.py`:** Wired the upstream API via the 8-branch reason enum (`cuda_unavailable`, `pre_ampere`, `user_disabled`, `engaged_warm_cache`, `engaged_cold_compile`, `compile_failed`, `probe_failed`, `pre_ampere_user_explicit`). D-25 decode-window invariant enforced as hard `AssertionError`. D-22 Branch A invariant assertion present. P-12 probe selects `_torchdynamo_orig_callable` on the talker's inner forward. Telemetry one record per call (`tts_compile_engaged`). 8/8 reason-enum test rows pass; both `__all__`-ordering pinned tests updated and pass.
* **Task 4 — ModelRegistry wire-up:** Stored `self._app_settings`; called `engage_compile_optimizations(model, app_settings=self._app_settings)` in `_load_model_sync` after `Qwen3TTSModel.from_pretrained` returns (wrapped in defensive try/except). Init INFO log line extended with `compile_engaged='deferred'`. New post-load INFO log line carries the resolved `compile_engaged` + `compile_reason`. 16/16 test_model_registry tests pass (14 pre-existing + 2 new).
* **Task 5 — `AppSettings.tts_compile`:** New field with three-valued domain (`auto` / `on` / `off`); validation block mirrors `tts_precision` exactly; `to_dict` / `from_dict` round-trip; `reset_to_defaults` field-list updated. 11/11 new `test_app_settings_tts_compile.py` rows pass.
* **Task 6 — `warmup_compile_async` + orchestrator wire-up:** Added the fire-and-forget worker with full gate logic (env-var, hardware, model-registry-wired, model-loaded). Cache-hit → silent INFO + `cache_hit` telemetry. Cache-miss → "Preparing TTS engine…" indicator → `_run_compile_priming` (dispatches `generate_custom_voice("Hello world.", speaker="Ryan")`) → `mark_warm` on success → indicator clears via `finally`. `app.py` wires the task after the hydrate task via `_run_async_task`. 6/6 warmup tests pass; Story 18.3 H1 static-scan tests 3/3.
* **Task 8 — NFR1 3-way A/B/C measurement harness:** Created `05/06/07_Story_18.4_NFR1_*.bat` (three branch loops with `MYVOICE_AUTO_QUIT_ON_CLOSE=1`), the `18-4-set-precision-and-compile.py` settings.json mutator, and the `18-4-aggregate-nfr1.py` 3-way aggregator (cold-compile run #1 discarded from branch A median; OFR-E gate + OQ #1 routing condition automatically surfaced). Commander runs the actual N=10 loops.
* **Task 9 helper — Audition helper script:** `18-4-l1-audition-helper.py` adapted from 18-3; points at `18-4-perceptual-fixtures/`; verdict-gate reminder updated for Story 18.4 framing.
* **Task 11 — Regression sweep:** 857/857 PASS on the Story 18.4-relevant scope (`tests/unit/services/`, `tests/unit/observability/`, `tests/unit/models/`, `tests/unit/test_app_qwen_tts_construction.py`, `tests/unit/test_app_progressive_playback.py`, `tests/integration/test_progressive_playback_dispatch_skip.py`, `tests/test_qwen_tts_internals.py`). Story 17.2 voice_clone_prompt cache (31/31) covers the pin-mismatch invalidation path via `test_pin_mismatch_invalidates_pt` — no new regression test needed since the existing test exercises the exact bug class against the new constant `3fdb4682`. Pre-existing failures in `tests/unit/ui/dialogs/voice_design_studio/` (30 failed + 3 errors) are unrelated to Story 18.4 — no UI code was touched.

**Commander-routed tasks (Tasks 7 bundled smoke, 8 N=10 measurement runs, 9 fixture regen + listener recruitment + audition, 10 architecture amendment, 13 code-review pass) — PENDING.**

* Task 7 (bundled smoke): waits on `build_release.bat` build under the new pin (`build_tools/verify_qwen_tts_pin.py` SHA-256 hashes need refreshing for the fork's `modeling_qwen3_tts.py` / `qwen3_tts_model.py` first).
* Task 8 N=10 runs: Commander launches `05/06/07_*.bat` for the three branches; dev agent computes deltas + OFR-E + OQ #1 routing on aggregator output.
* Task 9 fixture regen + audition: Commander regenerates `18-4-perceptual-fixtures/` via the production GUI, recruits ≥3 listeners, runs the audition via `18-4-l1-audition-helper.py`.
* Task 10 architecture amendment: contingent on Task 9 verdict (PASS or FAIL).
* Task 13 code-review pass: runs after status flips to `review`.

### File List

**New (source tree):**
* `src/myvoice/services/tts_streaming/compile_cache.py` — seven-dim compile-cache helper module.

**Edited (source tree):**
* `requirements.txt:23` — qwen-tts pin bumped to `dffdeeq/Qwen3-TTS-streaming@3fdb468233d73fa537202b94a1cc7c4e7a6160b8`.
* `build_tools/requirements-production.txt:61` — production-bundle pin bumped identically.
* `src/myvoice/services/qwen_tts_service.py` — `_QWEN_TTS_PIN_HASH` bumped to `"3fdb4682"`; added `warmup_compile_async` + `_run_compile_priming` methods.
* `src/myvoice/services/tts_streaming/torch_runtime.py` — added `engage_compile_optimizations` + `_probe_compile_engaged` + extended module docstring.
* `src/myvoice/services/tts_streaming/__init__.py` — appended `engage_compile_optimizations` + `compile_cache` to imports + `__all__`.
* `src/myvoice/services/model_registry.py` — stored `self._app_settings`; called `engage_compile_optimizations` after `from_pretrained`; extended INFO log with `compile_engaged='deferred'`; added post-load INFO log line.
* `src/myvoice/models/app_settings.py` — new `tts_compile: str = "auto"` field + validation + `to_dict` / `from_dict` round-trip + `reset_to_defaults` field-list entry.
* `src/myvoice/app.py` — wired `warmup_compile_async` fire-and-forget task after the hydrate task.
* `build_tools/verify_qwen_tts_pin.py` — updated `PINNED_COMMIT` + added `PINNED_REPO` constants; refreshed `KNOWN_GOOD_HASHES` for the two changed files at the new fork pin (`inference/qwen3_tts_model.py` and `core/models/modeling_qwen3_tts.py`); fixed the restore-command help text to use the new fork URL + `--no-deps`. `__init__.py` hash is byte-identical to the pre-bump pin.

**Edited (tests):**
* `tests/test_qwen_tts_internals.py` — `expected_methods` extended with `enable_streaming_optimizations`.
* `tests/unit/services/tts_streaming/test_torch_runtime.py` — added 8 `engage_compile_optimizations` rows + `_PlainObj` helper + `_isolated_inductor_env` fixture.
* `tests/unit/services/tts_streaming/test_codec_token_streamer.py` — `__all__` ordering test appended new symbols.
* `tests/unit/services/tts_streaming/test_streaming_decoder.py` — `__all__` ordering test appended new symbols.
* `tests/unit/services/test_model_registry.py` — `_StubAppSettings` widened with `tts_compile`; 2 new test rows for `_app_settings` storage + `compile_engaged='deferred'` init log.

**New (tests):**
* `tests/unit/services/tts_streaming/test_compile_cache.py` — 16 rows covering the 7 AC #3 sub-bullets.
* `tests/unit/models/test_app_settings_tts_compile.py` — 11 rows mirroring `tts_precision` discipline verbatim.
* `tests/unit/services/test_qwen_tts_service_compile_warmup.py` — 7 rows covering the AC #7 lifecycle (Task 13 added one row pinning the H1 `tts_compile="off"` gate).

**Code-review pass edits (Task 13, 2026-05-11):**
* `src/myvoice/services/qwen_tts_service.py` — H1: new `tts_compile="off"` gate in `warmup_compile_async`; M2: `_run_compile_priming` docstring corrected (Ryan + `generate_custom_voice`).
* `src/myvoice/services/tts_streaming/torch_runtime.py` — H2: real D-25 invariant via `decode_window_frames: Optional[int] = None`; M1: `qwen_tts_pin_hash: Optional[str] = None` parameter, dropped `services.qwen_tts_service` import; M4: `except Exception` cleanup.
* `src/myvoice/models/app_settings.py` — H3: `UNKNOWN_TTS_COMPILE` validator reset target → "off" matching field declaration.
* `src/myvoice/services/model_registry.py` — M1: reads `QwenTTSService._QWEN_TTS_PIN_HASH` and passes through as `qwen_tts_pin_hash=` to `engage_compile_optimizations`.
* `tests/unit/services/test_qwen_tts_service_compile_warmup.py` — added `test_warmup_tts_compile_off_skips_priming_and_gate_log` (H1 regression); `_make_service` injects `tts_compile="auto"` to preserve existing-test execution paths.
* `tests/unit/services/tts_streaming/test_torch_runtime.py` — added `test_engage_compile_d25_invariant_raises_on_explicit_window_mismatch` (H2 regression); warm-cache test updated to pass `qwen_tts_pin_hash=` through (M1 parameter propagation).
* `tests/unit/models/test_app_settings_tts_compile.py` — H3: validator-reset test renamed + updated to assert "off" (was "auto").

**New (evidence + measurement + audition + harness artifacts):**
* `_bmad-output/implementation-artifacts/18-4-torch-compile-decoder-persistent-cache-evidence.md` — D-22 verification + pin-bump rationale + P-12 probe selection (Bundled smoke / NFR1 / NFR3 sections pending).
* `_bmad-output/implementation-artifacts/18-4-set-precision-and-compile.py` — settings.json mutator for the 3 branches.
* `_bmad-output/implementation-artifacts/18-4-aggregate-nfr1.py` — 3-way A/B/C aggregator.
* `_bmad-output/implementation-artifacts/18-4-l1-audition-helper.py` — audition helper pointing at `18-4-perceptual-fixtures/`.
* `05_Story_18.4_NFR1_BF16_COMPILE.bat` (repo root) — Branch A (production target).
* `06_Story_18.4_NFR1_BF16_EAGER.bat` (repo root) — Branch B (bf16 baseline).
* `07_Story_18.4_NFR1_FP32_EAGER.bat` (repo root) — Branch C (strict baseline).

**Edited (sprint tracking):**
* `_bmad-output/implementation-artifacts/sprint-status.yaml` — `18-4-torch-compile-decoder-persistent-cache` flipped `ready-for-dev → in-progress → review` (after dev-agent-autonomous tasks closed).

**Edited (memories):**
* `C:/Users/AL301/.claude/projects/I--MyVoiceV2/memory/epic18_producer_bottleneck_finding.md` — chronological status section noting Story 18.4's source-tree landing; entry stays OPEN pending Commander measurement.
* `C:/Users/AL301/.claude/projects/I--MyVoiceV2/memory/build_tools_phase_perp_state.md` — recorded the qwen-tts pin bump to the dffdeeq fork; flagged the build-time pin-verification SHA-256 hashes for refresh before Task 7's bundled smoke.

**Out-of-scope (NOT part of Story 18.4's commit):**
* `build_tools/installer.iss:MyAppBuild` + `build_tools/version.py:VERSION_BUILD`: build counter `12 → 13` is a separate build-state commit per Story 18.2 / 18.3 OQ #4 precedent.

### Change Log

| Date | Change | Author |
|---|---|---|
| 2026-05-10 | Story created via `/bmad-bmm-create-story` workflow. Status: `ready-for-dev`. Architecture: implements D-21, D-22 (Branch B), D-23, D-24, D-25 + P-10, P-11, P-12 from `architecture-streaming-acceleration-and-lightning-tier.md` (sealed 2026-05-10). 13 tasks; 8 dev-agent-autonomous + 4 partially / fully Commander-routed (Tasks 7 bundled smoke, 9 audition, 10 architecture amendment contingent on 9, 12.1/12.2 conditional memory updates) + 1 post-implementation code-review pass. D-22 verification empirically pre-resolved 2026-05-10: Branch B fires (research subagent + dev-agent re-verification at Task 1.1); pin-bump from `QwenLM/Qwen3-TTS@1ab0dd75` to `dffdeeq/Qwen3-TTS-streaming@3fdb4682` (introducing commit "compile and fast codebook"). Joint audition framing per user direction 2026-05-10: single ≥3-listener cycle certifies bf16 + compile + pin-bump simultaneously (closes Story 18.3's deferred audition + D-22 Branch B re-audition + Story 18.4's primary perceptual gate). | scrum-master agent (Opus 4.7 [1m]) |
| 2026-05-10 | Dev-agent-autonomous tasks closed (Tasks 1, 2, 3, 4, 5, 6, 8 harness, 9 helper, 11). Status: `in-progress → review`. Source tree edits: 8 files edited + 1 new module (`compile_cache.py`); 3 new test files + 4 edited test files (~50 new test rows). Evidence file populated through §"P-12 capability probe selection"; Bundled smoke / NFR1 / NFR3 sections pending Commander-routed measurements. Pin bump live at `requirements.txt:23` + `build_tools/requirements-production.txt:61` + `_QWEN_TTS_PIN_HASH = "3fdb4682"`. Memory entries updated: `epic18_producer_bottleneck_finding.md` (status section added; entry stays OPEN until Task 8 measurement) + `build_tools_phase_perp_state.md` (pin bump recorded; `verify_qwen_tts_pin.py` hash refresh flagged). 857/857 PASS on Story 18.4-relevant regression scope (Story 18.3 closed at 803; Story 18.4 adds ~50 rows). Commander next: run `build_release.bat` (Task 7), run `05/06/07_Story_18.4_*.bat` × N=10 (Task 8), regenerate `18-4-perceptual-fixtures/` + recruit ≥3 listeners + run audition (Task 9). | dev agent (Opus 4.7 [1m]) |
| 2026-05-10 | `build_release.bat` Pre-Build Check failed: qwen-tts pin verification mismatch (script's `KNOWN_GOOD_HASHES` baked against `1ab0dd75`). Fixed `build_tools/verify_qwen_tts_pin.py` — `PINNED_COMMIT` updated to `3fdb468233d73fa537202b94a1cc7c4e7a6160b8`; added `PINNED_REPO = "dffdeeq/Qwen3-TTS-streaming"`; refreshed hashes for `inference/qwen3_tts_model.py` + `core/models/modeling_qwen3_tts.py` (`__init__.py` is byte-identical to old pin); restore-command help text now points at fork URL + `--no-deps`. Pin verification passes. | dev agent (Opus 4.7 [1m]) |
| 2026-05-10 | Bundled-smoke (post-pin-fix) installer-launch surfaced two regressions: (a) **double-launch** (splash → app → close → splash → app) caused by missing `multiprocessing.freeze_support()` — torch.compile inductor's subprocess workers re-exec the bundled exe; (b) **`ValueError: finalize() called with no chunks`** during generation — theorized as the fork's default `compile_talker=True` breaking Story 16.8's forward-hook. Fixes: `src/myvoice/main.py` gets `multiprocessing.freeze_support()` at the top; `engage_compile_optimizations` passes `compile_talker=False` to preserve Story 16.8's dispatch; `_probe_compile_engaged` retargeted from `talker.model.forward` to `talker.code_predictor.model.forward` (still compiled under `compile_codebook_predictor=True` default). Test factory updated; cold-compile test row extended with `compile_talker=False` assertion. 59/59 targeted tests pass. Commander: re-run `build_release.bat` to retry the bundled smoke. | dev agent (Opus 4.7 [1m]) |
| 2026-05-10 | Bundled-smoke (post-fix #1, with multiprocessing.freeze_support + compile_talker=False) re-installed: double-launch resolved, but generation STILL fails with the same `finalize() called with no chunks` ValueError. Log inspection at `build_tools/dist/MyVoice/logs/myvoice.log:311-419` revealed the real root cause: `torch._dynamo.exc.BackendCompilerFailed: ModuleNotFoundError: No module named 'torch._inductor.fx_passes.serialized_patterns'`. This is a PyInstaller hidden-imports gap — torch.compile lazy-imports inductor backend modules via `importlib.import_module(name_string)` at first-compilation, which PyInstaller's static analysis can't see. Fix #2: `build_tools/myvoice.spec` `hiddenimports_torch` extended with `collect_submodules('torch._inductor')` + `collect_submodules('torch._functorch')`. Bundle size will grow ~200-400 MB. Commander: rebuild via `build_release.bat`. If a 3rd regression surfaces, OQ #4 routing applies. | dev agent (Opus 4.7 [1m]) |
| 2026-05-10 | Bundled-smoke (post-fix #2) re-installed: double-launch only on first boot (multiprocessing footprint stabilizes after first compile pass); generation STILL fails with the exact same `ModuleNotFoundError` for `torch._inductor.fx_passes.serialized_patterns`. Inspection of `dist/MyVoice/_internal/torch/_inductor/fx_passes/` shows the entire `serialized_patterns/` subdirectory is missing despite `collect_submodules('torch._inductor')`. PyInstaller's package discovery skipped the subdirectory (likely due to its dot-prefixed module names: `_sfdp_pattern_1.py` through `_sfdp_pattern_24.py`). Fix #3: `build_tools/myvoice.spec` force-copies the 29-file `serialized_patterns/*.py` subtree as data via direct `glob.glob(...)` + enumerates each module as a hidden import + adds `torch_serialized_patterns_datas` to the `Analysis(datas=...)` arg. Commander: rebuild via `build_release.bat`; post-build verify `dist/MyVoice/_internal/torch/_inductor/fx_passes/serialized_patterns/__init__.py` exists. If a 4th regression surfaces, escalate to OQ #4 (user can also set `tts_compile="off"` in settings.json as NFR7 fallback to revert to Story 18.3 bf16-eager baseline). | dev agent (Opus 4.7 [1m]) |
| 2026-05-10 | Bundled-smoke fix-#3 spec landed a `_glob` NameError (PyInstaller built failed at spec-parse time because `_glob` was referenced before its `import glob as _glob` statement). Reordered imports in spec; PyInstaller build succeeded (`dist/MyVoice/MyVoice.exe` produced; serialized_patterns bundled with 28 .py files). Inno Setup ran (16 min LZMA2) and produced `installer_output/MyVoice-Setup-v2.1.0.exe` (2013.86 MB). Commander installed to `I:/MyVoice/`. Generation STILL fails: NEW error `Cannot find a working triton installation`. Triton-on-Windows blocker surfaced. Dev-env test with `triton-windows 3.6.0.post26` installed shows `tcc.exe` cannot link `cuda_utils.c` against portable-Python headers — beyond Story 18.4's scope. **OQ #4 routes.** Fix #4: default `AppSettings.tts_compile` from "auto" to "off"; bundle uses Story 18.3 bf16-eager baseline (already certified). All Story 18.4 source-tree machinery preserved (pin bump LIVE, compile_cache module LIVE, engage function LIVE, warmup worker LIVE, ModelRegistry wire-up LIVE) — bypassed at runtime by the "off" default. Users can opt-in via settings.json once Windows triton chain is resolved. Tasks 8 (NFR1) and 9 (NFR3) DEFERRED to a follow-up story. 11/11 tts_compile tests updated + pass; 129/129 Story 18.4 sanity-sweep tests pass. Commander: rebuild via `build_release.bat`. | dev agent (Opus 4.7 [1m]) |
| 2026-05-11 | **Task 13 — Code-review pass complete.** `/bmad-bmm-code-review` surfaced 3 HIGH + 4 MEDIUM + 3 LOW; auto-fixed H1/H2/H3 + M1/M2/M4 (3 LOW deferred as docs-only/non-load-bearing). **H1** (CRITICAL): `warmup_compile_async` had no `tts_compile` gate — with the Fix #4 default flipped to "off", a first-launch on Ampere+ CUDA would have fired `_run_compile_priming` → audible "Hello world." reaching the wired audio_chunk_ready_callback + meaningless meta.json sidecar poisoning the cache. New Gate 1b at `qwen_tts_service.py` reads `app_settings.tts_compile` and short-circuits with `reason="user_disabled"` on "off". **H2**: D-25 invariant assertion was tautologically true (computed `decode_window_frames` from RHS then asserted equality to RHS); replaced with real invariant — `engage_compile_optimizations` now takes `decode_window_frames: Optional[int] = None`; explicit values trigger hard `AssertionError` on streamer-window mismatch per P-11 "loud at startup". **H3**: `UNKNOWN_TTS_COMPILE` validator reset target diverged from field default; reset → "off" matching declaration (NFR7 safe-degradation discipline). **M1**: `torch_runtime.py` no longer imports `services.qwen_tts_service`; `engage_compile_optimizations` accepts `qwen_tts_pin_hash` parameter, `ModelRegistry._load_model_sync` reads the constant at the correct boundary layer. **M2**: `_run_compile_priming` docstring fixed (Ryan + `generate_custom_voice`, not Sarira-F + `generate_voice_design`). **M4**: redundant `except (TypeError, RuntimeError, AttributeError, Exception)` → `except Exception` cleanup. Two new regression tests pin H1 + H2 exact bug classes per `memory/code_review_regression_test_exact_class.md`. Second-pass re-audit confirmed no new regressions. Test surface: 88/88 PASS across the affected modules (test_torch_runtime + test_compile_cache + test_qwen_tts_service_compile_warmup + test_app_settings_tts_compile + test_model_registry); 97/97 PASS across the broader-sibling sweep (__all__ ordering + qwen_tts_internals + app_qwen_tts_construction + tts_precision parallel). Status stays `review` pending Commander-routed Tasks 7 (bundled smoke), 8 (NFR1 N=10 runs), 9 (NFR3 audition), 10 (architecture amendment) — all gated on the triton-on-Windows resolution per OQ #4. | dev agent (Opus 4.7 [1m]) |
