# Session handoff — 2026-05-11 → next dev

## TL;DR

- **Epic 18 closed.** Story 18.4 (torch.compile decoder + persistent compile cache) shipped FULL PASS. Producer-bottleneck ratio 3.23× → **0.670×** (OFR-E target met). NFR3 audition: 3 listeners × 10 utterances × A/B = 60 defect observations, zero defects flagged.
- **Branch `epic-16` is pushed and in sync with `origin/epic-16`** at `99b0def`. Working tree clean.
- **Story 18.5 is the natural next story.** Scope: ship CUDA Toolkit + Python 3.10.11 headers + triton-windows in the PyInstaller bundle so production users can flip `tts_compile="auto"`. All the source-tree machinery is LIVE; only packaging is missing.

## Read these first

1. **Story 18.4 file** — `_bmad-output/implementation-artifacts/18-4-torch-compile-decoder-persistent-cache.md` (the canonical closure record; all 13 tasks marked [x] except Task 7 bundled smoke which is deferred to Story 18.5).
2. **Story 18.4 evidence** — `_bmad-output/implementation-artifacts/18-4-torch-compile-decoder-persistent-cache-evidence.md` (full §"Bundled smoke (4-fix iteration)" + §"NFR1 first-chunk-latency measurement (3-way A/B/C)" + §"NFR3 joint audition verdict (FULL PASS)" + §"Triton-on-Windows dev-env smoke").
3. **Architecture follow-up note** — `_bmad-output/planning-artifacts/architecture-optimization-pass.md` §"Story 18.4 Follow-up Note (Joint bf16 + Compile + Pin-Bump Audition — FULL PASS, 2026-05-11)".
4. **Memory entries** (these auto-load):
   - `epic18_producer_bottleneck_finding.md` — CLOSED with the 0.670× ratio. Also documents the triton-on-Windows dev-env setup (Python headers + CUDA Toolkit + triton-windows install).
   - `build_tools_phase_perp_state.md` — has the HIGH follow-up for Story 18.5 packaging.

## State snapshot

```
Branch:        epic-16 (= origin/epic-16 @ 99b0def)
Working tree:  clean
Last 6 commits (this session):
  99b0def  Story 18.4 NFR3 audition L2 + L3 land — FULL PASS; Epic 18 closed
  a182391  Story 18.4 Task 10: architecture amendment (L1-partial form) — D-22 Branch B EXECUTED
  240b8a7  Story 18.4 Task 9: NFR3 joint audition — fixture regen + L1 partial PASS
  0a0c59f  Story 18.4 Task 8: NFR1 3-way A/B/C measurement — OFR-E producer-bottleneck target ACHIEVED
  3b727e5  build_tools: bump build counter to 15 for Story 18.4 bundled smoke iterations
  ca196b5  Story 18.4: torch.compile decoder + code-review pass + triton-on-Windows dev-env resolution
```

`AppSettings.tts_compile` default is `"off"` in the source tree (Fix #4 from 2026-05-10 bundled-smoke iterations). Story 18.5 is the gate to flipping it back to `"auto"`.

## Story 18.5 scope (concrete)

The dev-env triton-on-Windows resolution this session landed solved the *fundamental compatibility question* (verified via the 5-stage trivial smoke at `_bmad-output/implementation-artifacts/18-4-triton-smoke.py` and the 6-stage real-model smoke at `18-4-qwen-compile-smoke.py`). The remaining work is purely packaging:

1. **Bundle CUDA Toolkit redistributables** (~3 GB worth — `nvcc`, `cuda.h`, `cuda_runtime.h`, cuBLAS, cuDNN). NVIDIA's redistributable license terms permit this; the canonical pattern is in `cudart` and similar packages. Need to copy the relevant subset into the PyInstaller bundle.
2. **Ship Python 3.10.11 dev headers + libs** in the bundle's portable Python tree. The dev-env smoke needed `Include/Python.h` and `libs/python310.lib` copied from a full python.org install. Story 18.5 needs to bundle these so `pip install triton-windows` would work post-install (or pre-install them so users don't have to).
3. **Install triton-windows in the bundled site-packages** + enumerate its lazy-imported C extension paths as PyInstaller hidden imports / datas. Story 18.4's `myvoice.spec` already did this for `torch._inductor.fx_passes.serialized_patterns` (Fix #3); triton-windows likely has a similar lazy-import surface that PyInstaller's static analysis misses.
4. **Re-run the bundled smoke** end-to-end on a fresh install. Should produce the same 22-s cold compile + 1-s warm replay we measured in the dev-env real-model smoke.
5. **Flip `AppSettings.tts_compile` default back to `"auto"`** at `src/myvoice/models/app_settings.py:133` once the bundle path works. The audition is already pre-cleared.

Estimated work: significant. The bundle size will grow ~3-5 GB. Investigation may surface novel PyInstaller hidden-import gaps (triton has more dynamic-import surface than torch.compile). Story 17.2 / 18.4 iterations cycled through 3-4 hidden-import fix attempts each — expect similar for triton-windows.

## Triton-on-Windows dev-env setup (one-time per machine)

If the next dev hits "Cannot find a working triton installation" or similar on a fresh dev machine, the recipe is:

1. **Python 3.10.11 dev headers** — install Python 3.10.11 to a side location (e.g., `C:\Python310-fullinstall\`), copy `Include/` + `libs/` into `python310/` (the portable distribution).
2. **CUDA Toolkit 12.8** — install to the standard NVIDIA path; sets `CUDA_PATH` system-wide.
3. **triton-windows** — `python310/python.exe -m pip install --no-deps triton-windows` (currently 3.6.0).
4. **Smoke** — `python310/python.exe _bmad-output/implementation-artifacts/18-4-triton-smoke.py` (5-stage probe; expects all 5 to PASS).

## Open follow-ups (non-Story-18.5)

- **Epic 18 retrospective** — `/bmad-bmm-retrospective` is the natural workflow. Captures lessons across the 4-story arc. Not blocking anything.
- **rthook_torch.py debug log writes** — MEDIUM latent bug per `build_tools_phase_perp_state.md`. Cosmetic.
- **Inno Setup uninstaller empty parent key** — LOW per `build_tools_phase_perp_state.md`.

## Suggested first action for the next dev

Choose one of:

- **`/bmad-bmm-create-story`** to scaffold Story 18.5. The architecture decisions are already sealed (D-22 EXECUTED; D-23/D-24/D-25/P-10/P-11/P-12 all implemented). Story 18.5 is purely a packaging + bundled-smoke story.
- **`/bmad-bmm-retrospective`** to retro Epic 18 first. Capture the producer-bottleneck-close lesson (compile_talker=False as the bridge between Story 16.8 forward-hook + compile path) before it fades.
- **Hand-walk through `myvoice.spec`** to scope out the bundle changes for Story 18.5 before formally creating the story.
